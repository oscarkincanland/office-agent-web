# 地图阶段 3–6 执行记录

## 阶段 3：热力图与等时圈 Demo

- 地图顶部新增“义乌Demo”。
- 可生成义乌确定性演示点位热力图和 15/30/45 分钟演示等时圈。
- 结果以 MapLibre 运行时 source/layer 叠加，不整体刷新地图样式。
- 支持清除；通用分析结果可由面板保存为正式 GeoJSON 图层。

## 阶段 4：暹粒 OD

- 新增“暹粒OD”仪表盘。
- 支持起点热力、OD 期望线、流量阈值、起终点排名、统计卡和 JSON 导出。
- 当前内置为明确标记的 `siem-reap-od-demo.csv` 演示适配器，后续可替换为真实文件适配器。

## 阶段 5：新昌公交

- 新增“公交Demo”面板：公交线路、站点客流、公交 OD、线网统计四个 tab。
- 线路按城市/城乡/城际分色，站点按客流显示热力，OD 按流量显示紫色期望线。
- 当前内置 `xinchang-bus-demo` 确定性演示数据，接口结构已固定，后续可接入 GTFS/CSV/GeoJSON。

## 阶段 6：Agent 地图动作

- 新增 `map_analyze` 和 `map_clear_analysis` 工具。
- Agent 通过 `map_action` SSE 事件驱动地图局部更新。
- Agent 生成的演示结果显示来源标记；保存动作仍由用户在面板中确认，避免误写正式图层。

## 新增接口

- `GET /api/map/demo-analysis`
- `GET /api/demo/cambodia-od`
- `GET /api/m3/bus-routes`
- `GET /api/m3/station-heatmap`
- `GET /api/m3/od-lines`
- `GET /api/m3/network-stats`

## 验证

- `npm run build` 通过。
- `npm run test:phase5-9` 通过。
- `npm run test:closure` 通过。
- 三个新增接口在隔离本地服务上返回正常：义乌热力 36 点、暹粒 OD 记录可筛选、新昌公交 12 条线路/48 个站点/10 对 OD。

## 当前边界

- 暹粒和新昌目前是演示数据，不应作为真实交通结论；真实数据接入时复用同样的 GeoJSON/统计响应结构即可。
- 前端没有新增 ECharts 依赖，当前仪表盘采用原生 React/CSS，避免引入额外打包体积。
