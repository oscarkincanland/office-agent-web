# Open Plan（规聚）第一阶段修改计划与执行记录

> 文档用途：记录 Office Agent Web 的整体改造路线、第一阶段实施范围、验收标准和 Git 版本管理信息。
>
> 当前状态：阶段一至九的本轮实现已完成，本文档同时保留整体路线、阶段边界、验收结果和 Git 执行记录。

## 一、项目改造总目标

将当前依赖文本前缀和自然语言约定的 Agent 工作台，逐步升级为可追踪的任务系统：

```text
结构化引用
→ 明确任务上下文
→ 隔离会话
→ Agent 按需读取
→ 记录文件修改
→ 产物归属到任务
→ 历史可恢复
```

核心对象统一为：

- 工作区（workspace）：用户当前处理的项目目录。
- 会话（thread）：一段可恢复、可重命名的对话上下文。
- 执行（run）：一次用户指令触发的 Agent 运行。
- 引用（reference）：文件、目录、知识库文档、模板或文本选区。
- 产物（artifact）：本次执行新建或修改的文件。
- 技能（skill）：可被工作流调用的能力及其输入、输出和验证规则。

## 二、当前工作区保护原则

本轮已有本地改动不属于第一阶段功能范围，必须保留：

- `office-workspace/maps/zhejiang-map/map.config.json`
- `office-workspace/maps/zhejiang-map/style.json`
- `office-workspace/maps/zhejiang-map/layers/drawn-msotkp7p.geojson`
- `office-workspace/maps/zhejiang-map/layers/drawn-msvbd6cu.geojson`
- `.vscode/`
- `image/`

除非用户后续明确要求，不修改、删除或重置以上内容。

## 三、整体改造路线

### 阶段一：安全基线与版本管理

- 从 `develop` 创建独立功能分支。
- 服务默认只监听本机回环地址。
- 增加本地 API 访问保护。
- 限制任意 Office 命令、文件读写和工作区切换接口。
- 增加统一错误信息和请求追踪标识。
- 完成语法、构建和 API 冒烟验证。
- 更新版本说明和变更日志，形成可回滚提交。

### 阶段二：结构化多引用 `@`

- 建立统一 `Reference` 对象。
- 输入框上方增加引用栏。
- 支持多文件、多目录、多知识库文档、多模板同时引用。
- 支持 Markdown、HTML、Office 文档文本选区引用。
- 支持段落、标题、页码、单元格等范围信息。
- 增加 `/api/context/resolve` 和 `/api/context/read`。
- 增加 `context_read(refId, query?, range?)` Agent 工具。
- Agent 输出引用来源和未找到的引用。

### 阶段三：会话与工作区隔离

- 区分 `clientId`、`threadId`、`workspaceId`、`runId`。
- “新建会话”真正创建新的 Pi Agent session。
- 切换工作区时隔离或重建 Agent session。
- 历史恢复时恢复工作区、当前文件、引用和任务目标。
- 持久化 `ask_user` 待回答状态。
- 支持继续、重试和 fork 会话。

### 阶段四：外部文件统一读取

- 修复子目录文件访问和打开问题。
- 统一路径校验和文件句柄。
- 完整支持 docx、xlsx、pptx、pdf、csv、json、md、txt、html。
- 允许用户注册工作区外的本地目录。
- 为文件记录 mime、mtime、hash 和版本。
- 统一 Office、PDF、表格和文本抽取能力。
- 云端文件连接器作为后续独立阶段。

### 阶段五：产物与历史记录

- 增加 `run manifest`。
- 记录引用、读取文件、修改文件、工具调用和最终产物。
- 区分输入附件和 Agent 产物。
- 递归检测子目录、修改和删除。
- 支持文件 diff、版本和回滚。
- 产物按任务归属，而不是只显示当前目录最新文件。

### 阶段六：技能与工作流

- 统一技能注册表和版本信息。
- 校验 UI 显示的技能是否能被 Pi 实际加载。
- 将硬编码技能集合改为声明式工作流。
- 为每个步骤定义前置条件、工具、产出和验证方式。
- 对外部请求、大量写文件和破坏性修改增加确认。

### 阶段七：提示词与记忆

- 将项目规则、用户偏好、项目知识和任务临时状态分层。
- 引入统一 `TaskEnvelope`。
- 减少前端、`.agent-context.md` 和系统提示词重复注入。
- Agent 输出固定包含读取、修改、产物、假设和下一步。
- `memory_update` 改为 Agent 建议、用户确认后写入。
- 按 section 和 token 管理记忆，而不是直接截断文件尾部。

### 阶段八：前端体验

- 知识库、模板和地图面板共享统一引用栏。
- 保留聊天上下文，不因进入全屏模块而丢失任务状态。
- 增加当前任务状态条。
- 增加产物、引用、历史和文件修改之间的联动。
- 补充窄屏布局、键盘导航、ARIA 标签和弹窗焦点管理。

### 阶段九：测试与文档

- 增加引用解析、路径安全、会话隔离和产物 diff 测试。
- 增加 Agent SSE 断线恢复测试。
- 增加 Playwright 核心流程测试。
- 同步 `README.md`、`DESIGN.md`、`REQUIREMENTS.md`、`AGENT.MD` 和 `变更日志.md`。

## 四、第一阶段详细实施计划

### 4.1 分支和基线

- 当前分支：`feature/phase1-safety-baseline`
- 基线分支：`develop`
- 版本策略：第一阶段完成后以 `0.8.32` 作为补丁版本候选。
- 提交信息：使用 `类型: 描述` 格式，例如 `fix: 加固本地 API 访问边界`。

### 4.2 本机监听

将 Express 从默认监听地址改为显式监听 `127.0.0.1`，避免在局域网暴露文件和 Agent API。

允许通过环境变量覆盖：

```text
HOST=127.0.0.1
PORT=3001
```

默认仍然只监听本机；用户明确配置时才允许改变监听地址。

### 4.3 本地 API 保护

增加本地 token 机制：

- 服务启动时读取 `OAW_API_TOKEN`。
- 未设置 token 时，在本机开发模式下保持兼容，但输出安全警告。
- 设置 token 后，所有 `/api/*` 接口（状态接口除外）校验 `Authorization: Bearer <token>`、`x-oaw-token`、同源 HttpOnly Cookie 或 `?token=`。
- 浏览器可通过 `http://127.0.0.1:3001/?token=<OAW_API_TOKEN>` 引导同源 Cookie，现有 `fetch` 和 EventSource 调用无需逐个改造。
- 错误响应不泄露 API Key、绝对路径和内部堆栈。

### 4.4 文件和 Office 接口边界

第一阶段只做边界收紧，不改变 Office 功能：

- `/api/office` 仅允许 `view/get/set/batch/query/watch` 子命令，不再无条件接受任意命令参数。
- `/api/office` 拒绝绝对路径和 `..` 目录穿越参数，并固定以当前工作区为执行目录。
- 保留现有 `60mb` JSON body 上限；上传文件的扩展名和更细粒度额度校验放在后续文件引用阶段。
- Office 错误响应附带 `requestId`，便于从服务日志和前端反馈定位。

### 4.5 错误追踪

- 每个 API 请求生成 `requestId`。
- 当前阶段先统一返回请求 ID；请求耗时统计和前端错误面板放入后续可观测性阶段。
- 前端错误提示展示 requestId，便于定位。
- 保持用户文件内容和 API Key 不进入普通日志。

## 五、阶段二至四实施记录（2026-08-18）

### 阶段二：结构化多引用 `@`

- 新增 `server/context.mjs`，统一解析 `@文件[...]`、`@知识库[...]`、`@模板[...]`、`@模板目录[...]` 和带扩展名/子目录的普通 `@path`。
- 每个引用生成稳定 `ref_*` ID，并返回状态、相对路径、mime、mtime、hash、version 等元数据。
- 新增 `/api/context/resolve`、`/api/context/read`。
- 新增 Agent `context_read` 工具；prompt 会注入本轮引用摘要，Agent 按 ID 读取全文、关键词或行范围。
- 聊天输入增加引用栏，支持多文件/目录/知识库/模板引用并在用户消息中保留引用来源。

### 阶段三：会话与工作区隔离

- 前端新增 `threadId`，SSE、prompt、abort、answer、model 切换均按 `clientId + threadId` 隔离。
- “新建会话”调用 `/api/agent/new`，创建真正独立的 Pi session，不再只清空前端消息。
- 历史会话选择调用 `/api/agent/resume`，恢复对应 JSONL session；恢复失败时仍保留历史只读加载。
- 切换工作区自动创建新 thread，避免跨工作区复用上下文。

### 阶段四：外部文件统一读取

- `resolvePath` 使用 realpath + 工作区边界校验，支持子目录并拒绝绝对路径、目录穿越和符号链接越界。
- doc/raw/text/html/comments/watch/cells 路由支持嵌套文件路径。
- 文件列表和上传扩展名覆盖 docx、xlsx、pptx、pdf、csv、json、md、txt、html。
- 新增外部目录注册/删除 API：`GET/POST/DELETE /api/file-roots`。
- context reader 支持 docx/xlsx/pptx/pdf/csv/json/md/txt/html；PDF 优先调用系统 `pdftotext`，不可用时返回可解释提示。

### 阶段二至四验证

- `node --check server/index.mjs server/context.mjs server/agent.mjs server/workspace.mjs` 通过。
- `npm run build` 通过。
- `npm run verify` 通过。
- 手工验证：结构化引用解析、JSON 子目录读取、嵌套 `/api/doc/...` 路由和外部根目录列表接口。

### 当前边界

- PDF 的高质量文本抽取依赖系统 `pdftotext`；未安装时不会伪造内容。
- `ask_user` 的跨进程恢复仍需后续持久化 resolver/队列设计，当前已按 thread 隔离运行态待回答状态。
- 云端文件连接器、引用选区的 UI 级页码/单元格选择和完整历史 fork 操作放入后续阶段。

## 六、阶段五至九实施记录（2026-08-18）

### 阶段五：产物与历史记录

- 新增 `server/runs.mjs` 和 `/api/runs`，每次 Agent 请求生成持久化 `run manifest`。
- 记录 TaskEnvelope、引用摘要、执行事件、状态、产物和前后快照。
- 产物区分新增/修改/删除，并对小文件保存前后副本；支持 `/api/runs/:id/rollback` 显式确认回滚。
- 会话新增 `/api/sessions/:id/fork`，可从历史会话创建可恢复分支。

### 阶段六：技能与工作流

- 新增 `server/workflows.mjs` 声明式工作流注册表和 `/api/workflows`、校验接口。
- 工作流依赖技能可用性在启动前校验，前端使用 `@工作流[id]` 保留结构化工作流信息。

### 阶段七：提示词与记忆

- 新增 `server/task.mjs`，每轮生成统一 TaskEnvelope 并注入 Agent。
- 动态上下文固定要求输出“读取来源/修改文件/产物/假设/下一步”。
- 记忆按规则、项目信息、用户偏好、经验教训分层读取；`memory_update` 默认先生成待确认建议。

### 阶段八：前端体验

- 聊天顶部新增任务状态条，显示运行状态、run ID、引用和产物数量。
- 产物摘要支持变更类型、可回滚标识和显式回滚；记忆建议支持确认写入。
- 历史会话列表增加创建分支入口；技能工作流改为读取服务端注册表。

### 阶段九：测试与文档

- 新增 `npm run test:phase5-9`，覆盖引用解析、TaskEnvelope、工作流校验、产物快照和回滚。
- `npm run verify` 增加沙箱回环网络受限时的明确跳过提示；真实环境仍执行 API 冒烟测试。
- 同步 `AGENT.MD`、默认工作区 `AGENTS.md`、记忆初始化、README、设计文档和变更日志。

### 本轮剩余边界

- 云端文件连接器仍需按具体服务商单独接入和授权。
- PDF 复杂版式、扫描件 OCR、Office 图片/批注语义仍依赖专用解析器。
- `ask_user` 待回答问题已持久化到 `.oaw/pending-asks.json` 并提供查询接口；若原 Agent 进程已重启，仍需重新恢复会话后继续执行，无法凭空恢复内存中的 Promise。

## 七、第一阶段验收标准

### 必须通过

- `node --check server/*.mjs` 全部通过。
- `npm run build` 通过。
- `npm run verify` 通过。
- 服务默认只能通过 `127.0.0.1` 访问。
- 设置 token 后，未授权 API 请求返回 401/403。
- 现有工作区文件、地图文件和未跟踪文件没有被修改。
- Office 文档打开、预览和当前已有 Agent 调用不回归。

### 明确不在第一阶段范围

- 不实现结构化 `@` 引用。
- 不改变 Pi session 生命周期。
- 不重构知识库索引。
- 不重写技能工作流。
- 不实现云端文件连接器。
- 不修改地图数据和样式。

这些内容分别放在阶段二至阶段九，避免一次改动范围过大。

## 八、Git 版本管理流程

### 本轮

1. 在 `feature/phase1-safety-baseline` 完成第一阶段修改。
2. 运行语法、构建和 API 验证。
3. 更新本文件的执行记录和 `变更日志.md`。
4. 只提交第一阶段相关文件。
5. 检查 `git diff --stat` 和 `git status`，确认地图及用户本地文件未被带入提交。
6. 创建提交：

```text
fix: 加固本地 API 安全基线
```

### 后续合并

- 用户确认第一阶段结果后，再决定是否推送远程。
- 用户确认后再合并到 `develop`。
- `develop` 验证通过后再合并到 `main`。
- 稳定版本打 `v0.8.32` 标签。

## 九、执行记录

### 2026-08-17

- 已完成项目整体 review。
- 已识别当前工作区已有地图和文件改动，并列入保护范围。
- 已从 `develop` 创建 `feature/phase1-safety-baseline`。
- 已建立本计划文件。
- 已完成服务端安全基线：`HOST` 默认 `127.0.0.1`，支持 `PORT` 覆盖。
- 已完成可选 `OAW_API_TOKEN` 鉴权、同源 Cookie 引导和 `X-Request-Id` 响应头。
- 已收紧 `/api/office` 命令和路径边界，版本更新为 `0.8.32`。
- 验证通过：`node --check`、`npm run build`、`npm run verify`；Token 回归验证结果为未授权 `401`、Bearer `200`、URL Token 返回 `Set-Cookie`。
- 第一阶段提交：已完成，功能提交为 `bfc20e2`（`fix: 加固本地 API 安全基线`）。提交仅包含本阶段 5 个文件；地图、`.vscode`、图片和 GeoJSON 仍保留在工作区未提交状态。

### 2026-08-18

- 已完成阶段二至四实现：结构化引用、thread 会话隔离、外部文件统一读取。
- 版本更新为 `0.8.33`，语法、构建、API 冒烟和手工回归均已通过。
- 阶段二至四提交：已完成，提交 `c0ee7ca`（`feat: 完善引用会话与外部文件工作流`）。

### 2026-08-18（阶段五至九）

- 完成 run manifest、产物快照、变更类型、可回滚文件副本和历史会话 fork API。
- 完成声明式技能工作流注册、依赖校验、TaskEnvelope、分层记忆和记忆建议确认流程。
- 完成聊天任务状态条、产物变更摘要、回滚入口、技能工作流服务端同步和基础可访问性补强。
- 新增阶段五至九 smoke 测试，更新默认工作区行为准则和项目文档。
