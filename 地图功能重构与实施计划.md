# 交通规划地图功能重构与实施计划

> 状态：已确认，第一批实施中
>
> 计划版本：v0.8.33 之后的地图功能重构
>
> 目标：把现有地图从“图层展示与文件修改”升级为“地图工作台 + 分析 Demo + Agent 地图操作”的完整工作流。

## 一、重构原则

1. 页面面向业务使用，不再显示“M2”“M3”等内部阶段名称。
2. 演示数据和真实数据必须明确区分，页面显示数据来源、记录数和状态。
3. 分析结果采用临时图层管理，局部更新地图，不重复加载完整地图样式。
4. 浙江区域切换、地图标注、热力图、等时圈、OD 和公交分析使用统一的地图交互规范。
5. 保留当前已有地图、图层编辑、测量、绘制、导出和 Agent 能力，分阶段替换内部实现。
6. 每个阶段独立验证、独立提交，避免一次性重构造成无法回退。

## 二、产品功能定位

### 1. 地图工作台

- 底图切换；
- 浙江省 / 地市 / 县市区切换；
- 图层树管理；
- 图层显隐、排序、样式编辑；
- 行政区高亮；
- 属性查询；
- 测量和绘制；
- 标注管理；
- 报告图导出。

### 2. 道路与区域交通分析

- 路网流量分级；
- 道路等级和结构统计；
- 城市 / 县市区对比；
- 区域交换量；
- 主要交通走廊识别；
- 按区域、时段和数据集筛选。

### 3. OD 出行分析

- 起点 / 终点热力图；
- OD 期望线；
- 流量分级和 Top OD；
- 起点、终点和走廊排名；
- 流量阈值过滤；
- 地图与统计卡、排行榜联动；
- OD 结果保存和导出。

### 4. 公交线网与客流分析

- 公交线路展示；
- 城市、城乡、城际线路分类；
- 公交站点客流热力图；
- 公交 OD；
- 高峰 / 平峰筛选；
- 线路长度、覆盖和线网结构统计；
- 线路和站点标注；
- 点击线路、站点查看详情。

### 5. 热力图分析

- 点热力图；
- 网格聚合热力图；
- 区域分级热力图；
- CSV、Excel、GeoJSON 和粘贴表格导入；
- 经纬度字段自动识别；
- 流量 / 客流 / 数量字段选择；
- 时间段筛选；
- 半径、强度、透明度调节；
- Top 点位查看；
- 保存为正式图层。

### 6. 可达性与等时圈分析

- 15 / 30 / 45 / 60 分钟多档等时圈；
- 驾车、步行、骑行和公交模式；
- 地图选点和坐标输入；
- 等时圈中心点和时间标签；
- 等时圈内设施、道路和区域统计；
- 实时服务模式；
- 无外部 Key 时的演示模式；
- 结果保存为 GeoJSON 图层。

### 7. Agent 地图操作

支持自然语言指令：

- “在义乌市生成一个物流点热力图”；
- “切换到义乌市并显示县市区名称”；
- “叠加义乌市 30 分钟驾车等时圈”；
- “显示新昌公交站点客流热力图”；
- “分析暹粒市的主要 OD 出行走廊”；
- “把刚才的结果保存为地图图层”；
- “清除上一次分析结果”。

## 三、Demo 交付范围

### Demo A：浙江省交通地图

验证地图基础能力：

- 11 个地市和 90 个县市区切换；
- 当前区域自动定位和高亮；
- 地市、县市区和道路名称标注；
- 高速、国省道、收费站、枢纽图层；
- 义乌市热力图；
- 义乌市多档等时圈；
- 图层、标注、底图和分析结果联动。

### Demo B：柬埔寨暹粒市 OD 出行分析

使用柬埔寨项目中坐标完整的地图版 OD 数据，页面必须显示：

- 数据文件名；
- 数据记录数量；
- 总流量；
- 坐标匹配情况；
- 演示数据或真实数据状态。

交付内容：

- OD 总量和流量统计；
- 起点 / 终点 Top 排名；
- OD 期望线；
- 起点和终点热力图；
- 流量过滤；
- 地图与排行榜联动；
- OD 图层和报告图导出。

### Demo C：新昌公交线网与客流分析

参考当前新昌公交仪表盘，统一为一套数据适配器和接口。

交付内容：

- 公交线路分类；
- 公交线路名称标注；
- 站点客流热力图；
- 站点 Top 排名；
- 公交 OD；
- 线路长度和线网结构统计；
- 高峰 / 平峰筛选；
- 线路、站点和 OD 联动。

## 四、前端视觉规范

### 1. 整体视觉方向

- 地图画布保持浅灰白底，避免和卫星底图争夺注意力；
- 顶栏和分析面板使用深蓝灰色，形成稳定的工作台层级；
- 分析结果使用高辨识度色彩，但不改变行政区和基础道路的语义；
- 重要按钮使用单一主色，避免工具栏出现多种强烈颜色；
- 演示数据使用明显的“演示”标签，不伪装成真实分析结果。

### 2. 建议色板

| 用途 | 颜色 | 说明 |
|---|---|---|
| 工作台主色 | `#2563EB` | 主按钮、选中态、链接 |
| 工作台深色 | `#0F172A` | 顶栏、标题、深色面板 |
| 工作台辅助色 | `#64748B` | 次要文字、辅助说明 |
| 选中高亮 | `#FACC15` | 当前区域、当前要素 |
| 道路高流量 | `#DC2626` | 高流量路段 |
| 道路中流量 | `#F97316` | 中流量路段 |
| 道路低流量 | `#16A34A` | 低流量路段 |
| 公交线路 | `#2563EB` | 城市公交 |
| 城乡公交 | `#16A34A` | 城乡公交 |
| 城际公交 | `#F97316` | 城际公交 |
| OD 线路 | `#7C3AED` | OD 期望线 |
| 热力低值 | `#2D9CDB` | 热力图低值 |
| 热力中值 | `#F2C94C` | 热力图中值 |
| 热力高值 | `#EB5757` | 热力图高值 |
| 等时圈 | `#8B5CF6` | 可达性分析 |
| 成功状态 | `#16A34A` | 完成、有效 |
| 警告状态 | `#D97706` | 数据不足、演示模式 |
| 错误状态 | `#DC2626` | 请求失败、数据错误 |

热力图使用连续渐变，行政区、公交类型和交通状态使用离散色彩，避免相互混淆。

### 3. 图标规范

继续使用现有 `Icon` 组件，优先复用已有图标名称：

| 功能 | 图标语义 |
|---|---|
| 地图工作台 | `map` |
| 图层管理 | `layers` |
| 区域切换 | `locate` |
| 道路分析 | `road` |
| OD 分析 | `flow` |
| 公交分析 | `route` |
| 热力图 | `locate` 或数据点图标 |
| 等时圈 | `history` |
| 数据导入 | `upload` |
| 标注 / 绘制 | `penTool` |
| 统计 | `chart` |
| 保存 / 导出 | `download` |
| 刷新 / 重算 | `refresh` |
| 清除 | `trash` |
| 关闭 / 返回 | `close` / `back` |

图标规则：

- 工具栏使用 14px 左右线性图标；
- 仪表盘标题使用 16px 图标；
- 图标旁必须有文字，不使用难以理解的纯图标按钮；
- 加载、成功、警告、错误状态使用统一状态图标；
- 同一功能在顶部工具栏、面板和图例中保持同一图标语义。

### 4. 交互状态

统一设计以下状态：

- 默认；
- 悬停；
- 选中；
- 禁用；
- 加载中；
- 数据为空；
- 演示数据；
- 请求失败；
- 可撤销操作。

## 五、技术重构方案

### 1. 地图渲染核心

新增统一的分析图层管理机制：

- 基础图层由项目 `style.json` 管理；
- 分析图层由前端运行时管理；
- 热力图、OD、等时圈和 Agent 结果使用独立 source；
- 使用 `source.setData()` 更新数据；
- 不因一次分析结果整体 `reloadStyle()`；
- 结果支持临时显示、清除、撤销和保存。

### 2. 地图动作协议

Agent 不直接要求前端重载地图，而是返回标准地图动作：

```json
{
  "action": "show_analysis",
  "analysis": "heatmap",
  "project": "zhejiang-map",
  "region": "义乌市",
  "source": "demo",
  "fitBounds": true,
  "data": {}
}
```

前端根据动作执行：

- 定位区域；
- 创建或更新分析图层；
- 更新图例和统计面板；
- 显示数据来源；
- 等待用户确认后保存。

### 3. 数据适配器

统一以下数据结构：

- 点位数据；
- OD 数据；
- 道路流量数据；
- 公交线路数据；
- 公交站点数据；
- 等时圈结果；
- 行政区数据。

柬埔寨和新昌各自使用适配器，不把原始数据强行改造成互不兼容的结构。

### 4. 标注管理器

统一管理：

- 行政区名称；
- 道路名称；
- 公交线路名称；
- 点位名称；
- OD 流量标签；
- 等时圈时间标签；
- 用户绘制对象标签。

标注使用 MapLibre `symbol` 图层，并按缩放级别、优先级和碰撞规则显示。

## 六、实施阶段

### 阶段 0：基线与数据确认

- 保留现有未提交修改；
- 确认柬埔寨 OD Demo 数据源；
- 统一新昌公交数据源和 API；
- 确认热力图、OD、等时圈字段；
- 增加真实数据 / 演示数据标识。

验收：数据数量、坐标范围和来源均可核对。

### 阶段 1：地图视觉与标注基础

- 调整地图工具栏和分析面板配色；
- 统一图标语义和按钮状态；
- 增加地市、县市区标注；
- 增加基础点位和道路标注；
- 增加图例和状态提示。

验收：地图标注清晰，底图切换后仍可读，工具栏视觉统一。

### 阶段 2：区域切换与地图状态

- 增加省 / 市 / 县市区选择器；
- 增加区域搜索；
- 自动定位和边界高亮；
- 分析结果跟随当前区域；
- 保持缩放和相机状态。

验收：选择义乌后可自动定位、标注、高亮，并恢复全省视图。

### 阶段 3：热力图与等时圈 Demo

- 完善真实数据导入；
- 增加演示数据生成；
- 增加点热力和网格热力；
- 增加多档等时圈；
- 增加等时圈演示模式；
- 增加统计卡、图例和结果保存。

验收：义乌热力图和等时圈可独立叠加、清除、重算和保存。

### 阶段 4：柬埔寨 OD 仪表盘

- 接入柬埔寨 OD 数据适配器；
- 完成暹粒市 OD 线和起终点热力；
- 完成排名、筛选和统计联动；
- 增加数据来源说明和导出。

验收：可以完成一次从数据加载到地图分析图导出的完整流程。

### 阶段 5：新昌公交仪表盘

- 统一新昌公交接口和数据目录；
- 完成线路、站点、公交 OD 和结构统计；
- 增加线路、站点和客流标注；
- 增加高峰 / 平峰筛选；
- 增加地图与图表联动。

验收：线路数量、站点数量和统计结果来源一致。

### 阶段 6：Agent 地图操作

- 增加区域识别；
- 增加分析类型识别；
- 增加数据源判断；
- 增加地图动作事件；
- 支持生成、叠加、保存、清除和撤销。

验收：用户说“在义乌生成热力图”后，中间地图可直接显示结果且不整体刷新。

### 阶段 7：性能、兼容与版本收口

- 大数据量热力图聚合；
- GeoJSON 简化和分级加载；
- 结果图层清理；
- 请求取消和缓存；
- 前端构建和 API 冒烟测试；
- 手工回归三套 Demo；
- 更新变更日志和版本号。

## 七、版本管理安排

建议每个阶段单独提交：

1. `feat: 地图视觉与标注基础`；
2. `feat: 浙江行政区切换`；
3. `feat: 热力图与等时圈 Demo`；
4. `feat: 柬埔寨 OD 分析 Demo`；
5. `feat: 新昌公交分析 Demo`；
6. `feat: Agent 地图动作`；
7. `perf: 地图分析图层性能优化`；
8. `docs: 更新地图功能重构计划与变更日志`。

版本号沿用当前版本顺序递增。每次提交前执行：

- `npm run verify`；
- 前端构建检查；
- API 接口检查；
- GeoJSON 记录数和坐标检查；
- 三套 Demo 手工回归；
- `git diff` 和工作区状态检查。

## 八、最终验收标准

- 地图有统一的图标、配色和状态反馈；
- 地市、县市区和主要数据要素有清晰标注；
- 可以切换到任意浙江县市区并配合底图分析；
- 可以生成义乌热力图并叠加等时圈；
- 可以查看柬埔寨暹粒 OD 分析 Demo；
- 可以查看新昌公交线网与客流分析 Demo；
- Agent 可以根据自然语言在地图中生成分析结果；
- 分析结果更新不导致地图整体刷新；
- 演示数据和真实数据清楚区分；
- 结果可以清除、撤销、保存和导出；
- 所有阶段均有 Git 提交和验证记录。
