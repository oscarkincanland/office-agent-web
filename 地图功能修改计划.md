# 地图（GIS）功能修改计划

> 本文档记录地图 GIS 功能的调研结论、分期规划与实施状态，随版本迭代更新。
> 当前基线：v0.7.6（feature/0.7.5-map-panel）

## 一、需求背景

在 Office Agent Web（交通规划办公工具）中新增地图展示与编辑能力：

- 展示浙江省高速公路、国省道、农村公路、区县/地市行政区划矢量数据
- 前端三栏布局：工具栏 + 图层文件树 / 地图主区 / agent 对话栏
- agent 可通过对话修改地图内容，修改实时反映到地图
- 开源底图 + 外部 API（等时圈分析等）
- 交互体验对标 QGIS 的图层管理与制图界面

## 二、参考项目调研（2026-08-11）

| 项目 | 技术栈 | 借鉴点 |
|---|---|---|
| geosolutions-it/MapStore2 | React + OL/Leaflet/Cesium | TOC 图层面板：分组/拖拽/搜索/右键/图例 |
| qgis/qwc2（QGIS 官方 Web 客户端） | React + OpenLayers | 图层树、图例、要素查询 |
| maplibre/maputnik | React + MapLibre | 逐层样式编辑、拖拽排序 |
| keplergl/kepler.gl | React + deck.gl | 图层组、复制图层、按字段着色 |
| opengeos/maplibre-gl-layer-control | React + TS | 类型符号推断、分类型样式控件、底图分组 |
| antvis/L7 | WebGL | 热力图/3D/OD 流可视化（远期） |

技术路线：继续 MapLibre GL + React 自研，借鉴上述项目交互设计，不引入重框架。

## 三、分期规划与实施状态

### P0 — QGIS 式图层面板 ✅ 已完成（v0.7.5）

- [x] 图层分组（基础地理/路网/设施点，config.layers.group，可折叠、整组显隐）
- [x] 拖拽排序（HTML5 DnD，跨组/组内，实时写入 style.json）
- [x] 图例符号缩略图（从 paint 推断线/点/面 SVG）
- [x] 右键菜单（属性表/缩放/重命名/复制/删除）
- [x] 样式编辑器（分类型控件：颜色/线宽/透明度/线型/半径/描边）
- [x] 属性表弹窗（排序/搜索/行定位/分页/导出 CSV）
- [x] 图例展开（要素数/字段/样式摘要）

### P1 — 亮眼功能 ✅ 已完成（v0.7.6）

- [x] 测量工具（距离/面积，实时计算）
- [x] 绘制工具（点/线/面 → 命名保存为新图层）
- [x] 按字段样式化（分类渲染 match / 分级渲染 step，调色板+图例预览）
- [x] 等时圈多档对比（30/60/90 分钟叠加）

### P2 — 远期规划（待排期）

- [ ] 标注渲染（text-field，选中字段显示名称标签）
- [ ] OD 流量热力图 / 动线（L7 或 maplibre 热力层）
- [ ] 3D 地形/建筑
- [ ] 打印/报告导出（地图+图例+比例尺排版）
- [ ] 数据版本管理（图层更新历史、回滚）
- [ ] 路线规划（驾车/公交路径叠加）

## 四、数据管线（现状）

| 图层 | 数据来源 | 状态 |
|---|---|---|
| 地市边界/区县边界 | scripts/fetch-boundaries.mjs（DataV GeoAtlas） | ✅ 已生成 1534 瓦片 |
| 高速公路 | 用户矢量数据 → scripts/prepare-map-data.mjs | ⏳ 等待用户提供数据 |
| 国省道 | 同上 | ⏳ 等待用户提供数据 |
| 农村公路 | 同上 | ⏳ 等待用户提供数据 |
| 收费站/枢纽 | 同上 | ⏳ 等待用户提供数据 |

生成命令：`node scripts/prepare-map-data.mjs [数据源目录] && node scripts/build-vector-tiles.mjs`

## 五、外部服务配置

- **等时圈分析**：高德 Web 服务 Key，环境变量 `AMAP_KEY`（服务端代理 /api/map/isochrone）
- 后续地理编码/POI/路径规划复用同一 Key 通道

## 六、验证方式

- `npm run verify`：语法检查 + 前端构建 + API 冒烟
- 浏览器手动回归：地图模式进入/图层树/拖拽/样式编辑/属性表/测量/绘制/等时圈
