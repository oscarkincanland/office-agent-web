# 版本管理计划

## 一、版本号规则（语义化版本）

采用 `主版本.次版本.补丁版本` 三段式：

- **主版本 (major)**：不兼容的架构/接口变更，如重构前后端协议
- **次版本 (minor)**：新增功能，向后兼容，如新的插件、新的面板
- **补丁版本 (patch)**：Bug 修复、性能优化，如滚动修复、白屏修复

当前基线：**v0.6.0**（0.x 阶段表示功能仍在快速迭代）

## 二、分支策略（轻量 trunk-based）

项目为单机工具型应用，采用轻量分支策略：

```
main     ← 稳定分支（始终可运行），替代原 master
   │
   └── develop  ← 开发分支（当前活跃）
          │
          └── feature/xxx  ← 功能分支（每轮开发在此进行）
```

| 分支类型 | 命名规则 | 用途 |
|---------|---------|------|
| main | `main` | 稳定基线，每次合并后打 tag |
| develop | `develop` | 开发分支，功能汇总处 |
| 功能分支 | `feature/xxx` | 每轮新增功能/修复独立分支 |
| 修复分支 | `fix/问题名` | 紧急 bug 修复 |

**每次修改流程：**
1. 从 develop 拉新分支 `feature/xxx` 或 `fix/xxx`
2. 修改代码
3. 跑验证：`npm run verify`（构建 + 语法检查 + 冒烟测试）
4. 更新「变更日志.md」
5. 合并回 develop；稳定后合并 main 打 tag `v0.x.y`
6. 报告验证结果

## 三、验证与测试体系

### 3.1 验证脚本（scripts/验证脚本.mjs）

| 检查项 | 命令 | 内容 |
|--------|------|------|
| 语法检查 | `node --check` | 校验 server 全部 .mjs 文件 |
| 前端构建 | `npm run build` | vite 构建，校验 JSX/ESM 无编译错误 |
| 冒烟测试 | `node scripts/验证脚本.mjs` | 启动服务器，检查 API 端点 |

### 3.2 手工回归清单（每轮修改后人工确认）

- [ ] 打开 .md 文件可滚动，目录浮窗可定位标题
- [ ] .docx 可预览/实时预览，批注可显示、可定位高亮
- [ ] 发送对话后页面不白屏，流式输出正常
- [ ] 对话总结中的文件名可点击打开
- [ ] 文件侧栏右键菜单可用，文件管理器可打开
- [ ] 新建/修改文件后列表刷新

## 四、变更日志管理

- 变更日志文件：`变更日志.md`
- 每次功能合并/修复完成后必须更新
- 格式：按版本倒序，记录 Added / Fixed / Changed / Removed

## 五、当前状态与后续计划

| 版本 | 内容 | 状态 |
|------|------|------|
| v0.1.0 | 初版（对话流/工作区/历史/MD/Excel） | 已发布 |
| v0.2.0 | MD目录浮窗、对话白屏修复、批注定位、文件右键菜单、对话产物点击打开 | 已发布（main） |
| v0.3.0 | 图标统一、主题切换、头像、技能广场分类+工作流、会话按文档过滤、附件导入 | 已发布 |
| v0.3.1 | 智能体广场(4助手)、AGENT.MD长期记忆、Word式批注、滚动条统一、最近会话置顶 | 已发布 |
| v0.3.2 | 工作计划展示板、推理强度选择、Agent报错竞态修复、创作产物写工作区、批注Word风格重设计、对话设置栏下移 | 已发布 |
| v0.4.0 | 顶部工具栏(编辑/预览)、对话输入区重设计、批注可展开、文本编辑保存、历史兼容pi v3、批注精确定位 | feature/v0.4.0 WIP |
| v0.5.0 | Office Viewer集成（docx-preview批注修订渲染 + pptxviewjs翻页）、raw文件流接口、文档路由正则化修复 | 分支完成，待合并 develop |
| v0.6.0 | 知识库全屏模式(Obsidian风格)、知识图谱(G6力导向相似边)、本地索引+全文搜索、IMA云端集成、kb_search/kb_read agent工具、@引用分析链路、根目录管理 | feature/knowledge-base 开发中 |

## 六、注意事项

1. 每次合并前必须跑 `npm run verify`，失败则不允许合并
2. tag 命名：`v主.次.补丁`（如 `v0.2.0`）
3. 提交信息规范：`类型: 描述`（feat/fix/chore/docs/refactor）
4. 不得直接提交到 master，必须经过 feature 分支
