# 变更日志

本项目遵循[版本管理计划.md]中的语义化版本规则。所有值得注意的变更都会记录在此文件中。

## [v0.7.6] - 2026-08-11（feature/0.7.5-map-panel，开发中）

### Added（新增）
- **测量工具**：顶栏「测量」菜单（距离/面积），点击地图加点实时计算（Haversine 距离 / 纬度修正平面面积），双击完成，结果浮层显示，Esc 取消
- **绘制工具**：顶栏「绘制」菜单（点/线/面），绘制完成后命名保存为新图层（自动重建瓦片、按类型分组），支持 Esc 取消
- **按字段渲染**（样式编辑器扩展）：单一颜色 / 按字段分类（match 表达式，唯一值≤8+其他）/ 按数值分级（step 表达式，3-6 级）；字段自动探测（分级模式只列数值字段）、4 套预置调色板、图例色块预览，一键应用/恢复
- **等时圈多档对比**：勾选「多档对比」后同时计算多档时间（如 30/60/90 分钟），深→浅紫色叠加显示
- MapViewer 新增 setDrawingMode：绘制/测量模式下禁用属性弹窗、光标变十字

### Changed（变更）
- 绘制/测量使用临时地图图层（draw-temp / iso-temp*），不写入项目，导出/重建瓦片不受影响

## [v0.7.5] - 2026-08-11（feature/0.7.5-map-panel，开发中）

### Added（新增）
- **QGIS 风格图层面板**（LayerPanel 重构）：
  - 图层分组：基础地理 / 路网 / 设施点（config.layers 新增 group 字段，导入图层按类型自动归类），组头可折叠、整组显隐开关、图层计数
  - 拖拽排序：HTML5 原生 DnD 跨组/组内调整叠放顺序，实时写入 style.json
  - 图例符号缩略图：从图层 paint 属性自动推断线/点/面 SVG 小图标（颜色/线宽/半径实时反映样式）
  - 右键菜单：属性表 / 缩放到图层 / 重命名（行内双击也可编辑）/ 复制图层 / 删除图层
  - 图例信息展开：要素数、属性字段列表、样式摘要（颜色/线宽/透明度）
  - 样式编辑器：选中图层后底部展开，分类型控件（线：颜色/线宽/透明度/线型预设；点：颜色/半径/描边/透明度；面：填充色/边框/透明度），修改即保存 style.json 并热更新地图
- **属性表弹窗**（AttributeTable）：字段表格（列点击排序）、属性值搜索过滤、行点击定位到地图（fitBounds）、分页（每页 100）、导出 CSV（含 BOM）

### Changed（变更）
- MapPanel 左栏由扁平列表升级为 LayerPanel；旧图层行操作（上下移按钮/内联滑杆）移除，由拖拽+样式编辑器替代
- LAYER_DEFS 增加 group 字段；map.mjs defaultConfig / importLayer 自动填充分组

## [v0.7.4] - 2026-08-11（feature/0.7.4-map-gis，开发中）

### Added（新增）
- **地图全屏模式（GIS 项目）**：与知识库/模版库同款全屏布局（顶部栏「地图」按钮进入），三栏结构——左侧图层文件树、中间地图（MapLibre GL 矢量瓦片）、右侧 agent 对话栏
  - 图层文件树：显隐开关、透明度滑杆、上移/下移叠放顺序、缩放到图层、删除图层（含瓦片）
  - 顶栏工具栏：项目切换、开源底图切换（Carto 亮/暗、OSM、Esri 卫星）、重建瓦片、导出高清 PNG
  - 地图主区：要素点击属性弹窗、坐标/缩放状态栏、GeoJSON/SHP 导入（shpjs 解析，自动生成瓦片）
  - 等时圈分析弹窗：出行方式（驾车/步行/骑行/公交）+ 时间范围 + 地图选点，调用高德 v5 isochrone（服务端代理，需配置环境变量 AMAP_KEY），结果以临时紫色多边形叠加显示
- **矢量瓦片数据管线**：`scripts/fetch-boundaries.mjs`（DataV GeoAtlas 拉取浙江省 11 地市 + 90 区县边界）、`scripts/prepare-map-data.mjs`（按文件名关键词自动匹配高速公路/国省道/农村公路/收费站/枢纽，兼容 OSM 标签混合路网单文件）、`scripts/build-vector-tiles.mjs`（geojson-vt + vt-pbf 生成 pbf 瓦片）
- **农村公路图层**：LAYER_DEFS 新增 roads-rural（绿色线样式，z7-14）
- **地图服务端 API**：/api/map/projects、/project、/style、/config、/import（GeoJSON→瓦片）、/rebuild、/layer（读取/删除）、/isochrone，静态挂载 /api/map/data/*（style.json/瓦片/图层数据）
- **Agent 地图工具**：`map_read`（读项目配置/图层/样式）、`map_edit`（改图层显隐/颜色/线宽/透明度/顺序/新增样式图层，实时反映到前端地图）、`map_import`（工作区 GeoJSON 导入为新图层）；agent 上下文注入地图项目说明
- 浙江边界默认数据已就绪：boundary-city（142 瓦片）+ boundary-county（1392 瓦片）

### Fixed（修复）
- **应用白屏修复**：pptxviewjs 依赖的 chart.js（可选 peer）缺失导致 vite dev 构建失败/生产包运行时 `undefined.from` 崩溃，补装 chart.js
- **shpjs 浏览器兼容**：immediate 包引用全局 `global`、safe-buffer 依赖 Node 内置 buffer——vite 配置 `define: { global: "globalThis" }` + buffer polyfill alias
- **图层清单误列 macOS 垃圾文件**：map.mjs 过滤 `._*` 前缀文件
- **等时圈选点被弹窗遮挡**：选点模式下 backdrop 置为 pointer-events: none，允许点击地图选中心点

### Changed（变更）
- 前端依赖新增：maplibre-gl、shpjs、chart.js、buffer；服务端新增：geojson-vt、vt-pbf

## [v0.7.3] - 2026-08-11（feature/0.7.x-workspace-fix）

### Added（新增）
- **知识库文件树懒加载**：server/kb.mjs 新增 `getTreeLevel`（按路径返回直接子级），前端按需展开，目录/文件行带计数徽标（子项数/引用数），hover 行内 @ 到对话操作，排序切换（按名称/修改时间）
- **知识库展开状态持久化**：文件树展开目录状态存入 localStorage（按根目录分 key），切换根目录自动恢复；打开文档自动展开祖先链并滚动定位（对齐 siyuan selectItem/setCurrent）
- **反链上下文预览**：server/kb.mjs `getDoc` 反链附带引用段落上下文（大小写不敏感匹配），右栏反向链接可点击展开/收起引用上下文
- **标签交互**：笔记内 #tag 下划线样式可点击搜索（MarkdownBody 新增可选 onTagClick，对齐 siyuan span[data-type=tag]）；文档信息区/搜索结果标签可点击搜索；图谱标签节点点击 → 执行搜索（对齐 siyuan）
- **图谱控制条**：搜索节点定位输入框 + 隐藏孤立节点开关（对齐 siyuan pruneUnref）

### Fixed（修复）
- **工作区列表混入 macOS AppleDouble 垃圾文件**：server/workspace.mjs `listWorkspace` 增加 `.` 前缀过滤（`._xxx` 元数据文件 / `.DS_Store`），文件树与产物面板不再显示，避免误点打开二进制乱码
- **产物面板打开即白屏**：client/src/components/SessionSidebar.jsx 产物 tab 调用了未定义的 `formatRelTime`，补充实现（复用 formatTime 相对时间逻辑），产物列表恢复正常显示
- **知识库根目录切换竞态**：loadRoot 清空 treeRoot 时反复触发初始加载 effect，导致切换到新根后立即被重置回根 0；改为 initRef 一次性初始化 + 切换序号防竞态

### Changed（变更）
- **知识图谱 Obsidian/siyuan 风格重构**（client/KnowledgeGraph.jsx）：
  - similar 内容相似边默认关闭（graphInc 默认 ["links","tags"]），阈值提高（common≥12、overlap≥0.25、上限 ≤2n）
  - hover 节点 → 邻接节点+边高亮、非邻接降透明（Obsidian 核心交互）
  - 节点大小 ∝ 连接度（关联越多越大）；file 节点按顶层目录分组着色（组间色相区分，tag/目录保持专属色）
  - 缩放联动：缩小到阈值以下自动隐藏标签文字（防密）
  - 节点 >500 分批渲染（对齐 siyuan 分批策略）
- 图谱边样式区分：link 实线清晰、similar 细线淡显

## [v0.7.2] - 2026-08-11（feature/fix-products-graph-tpl）

### Added（新增）
- **模板库文件预览**：右侧预览面板支持 Word/PDF/PPT/Excel 文件（iframe 渲染，通过 officecli 服务端渲染）
- **模板库排序**：支持按类型（默认）/名称/大小三种排序方式

### Fixed（修复）
- **产物点击显示失败**：App.jsx `open` 函数合并为单次 `setTabs`，修复 React 18 批处理导致重复 tab 且第一个 tab 缺少 doc 属性的问题
- **prompt 出错时产物丢失**：server/index.mjs 在 catch 分支也执行 `waitForFlush` 并发射 `agent_summary`，确保 agent 已写入的文件仍显示在产物列表

### Changed（变更）
- **知识图谱优化**：Obsidian 风格纸面背景（minor 24px + major 120px 半透明格线），增大节点间距（linkDistance 180/280，nodeStrength -600），修复深色主题下字体/背景颜色对比度，优化悬停高亮和边透明度

## [v0.7.1] - 2026-08-10（feature/v0.7.1-editor-enhance，待确认后合并）

### Added（新增）
- 增强编辑工具栏：更多字体选项（20+）、颜色直接显示色块预览（20种常用颜色）、标注颜色/高亮（10种）
- 批注面板：点击批注按钮显示文档批注列表
- 表格支持优化

### Fixed（修复）
- 修复批注显示：renderComments 强制启用，添加独立批注面板
- 修复目录导航：启发式提取标题（兼容非标准样式）

## [v0.6.0] - 2026-08-10（feature/knowledge-base，开发中）

### Added（新增）
- **知识库全屏模式**：Obsidian 风格知识库界面，独立全屏模式切换（顶部栏「知识库」按钮），含文件树浏览、Markdown 预览（复用 MarkdownBody）、文档信息面板（标签/双向链接/反链/标题结构）
- **知识库 Obsidian Tab**：多文档标签页（点击文件开 tab，支持切换/关闭/缓存），左栏上下分割（文件树+文档信息可拖拽），@ 到对话按钮（自动退出知识库模式→延迟插入 ChatPanel）
- **知识图谱（思维图谱）**：基于 antv G6 v5 力导向图，节点=文档（41+），边=内容相似度（自动计算 bigram 重叠，阈值≥8 且 overlap≥0.16），支持标签/目录/双向链接三种可选边类型，点击节点联动预览，图例/布局控制/缩放/拖拽
- **本地知识库索引服务（server/kb.mjs）**：扫描配置根目录的 .md 文件，解析 frontmatter/title/tags/[[wikilink]]/内联 #tag，建全文子串搜索索引（加权打分：标题×10/标签×6/标题行×4/正文×1）+ 内容相似边图谱
- **IMA 云端知识库集成**：可选代理腾讯 ima OpenAPI（凭证配置后自动连接，支持知识库列表/搜索/读取原文/笔记正文；未配置或凭证失效时返回引导提示）
- **KB API 路由**：/api/kb/status（索引状态）, /api/kb/roots（增删根目录）, /api/kb/tree（目录树）, /api/kb/search（全文搜索）, /api/kb/graph（图谱数据）, /api/kb/doc（文档详情含反链）, /api/kb/ima/*（IMA 代理）
- **Agent KB 工具**：自定义工具 `kb_search`（知识库全文搜索）+ `kb_read`（读取文档全文），通过 ResourceLoader 上下文注入使用说明，agent 可主动检索知识库资料进行分析
- **@ 引用分析链路**：知识库文档「到对话」按钮插入 `@知识库[路径@根目录名]`，agent 识别后调用 kb_read 工具读取全文进行分析（自动退出 kbMode 延迟插入）
- **根目录管理**：kb.json 配置文件持久化，前端支持添加/移除知识库根目录（不删除原文件）
- **文件树浏览**：递归目录树，文件类型图标（Icon.jsx 补 back/cloud/backlink 图标），点击文件预览，搜索结果列表模式，最近打开定位高亮
- **模版库全屏模式**：交通规划产出文件模版库（147 个模版，10 个分类：公文版面/工作汇报/研究报告/规划报告/行业研究/论文写作/会议通知/调研问卷/研究框架/图表地图），支持网格/列表视图切换，md 文件渲染预览，二进制文件可在办公模式中打开
- **模版库 API 路由**：/api/templates（分类列表+筛选）, /api/templates/content（内容预览）, /api/templates/refresh（重新扫描）

### Changed（变更）
- App.jsx 新增 kbMode 全屏模式状态（KnowledgeBase 组件渲染），与三栏布局互斥；顶部栏新增「知识库」按钮（grid 图标）
- api.js 新增 kb 系列 API 函数（kbStatus/kbTree/kbSearch/kbGraph/kbDoc/kbIma*）
- agent.mjs customTools 数组扩展：[officeTool, kbSearchTool, kbReadTool]；ResourceLoader 上下文新增知识库使用说明
- client/package.json 新增依赖 @antv/g6 ^5.1.1
- Icon.jsx 新增 back/cloud/backlink 三个图标

## [v0.5.0] - 2026-08-10（feature/v0.5.0-office-viewer，待确认后合并）

### Added（新增）
- Office Viewer 集成（对标 VSCode cweijan.vscode-office 插件）：
  - **docx-preview**：Word 高保真渲染（分页/表格/图片/样式），支持 `renderComments`（文档内批注显示）与 `renderChanges`（修订痕迹显示），可切换
  - **pptxviewjs**：PPT Canvas 渲染，支持翻页/跳页/页码导航
  - 新增 `/api/doc/:file/raw` 原始文件流接口（正则路由，兼容中文文件名）
- 文档路由全部改为正则路由（修复 Express 5 中 `:file` 吞掉 `/raw` 等后缀导致中文文件名匹配失败的问题）

## [v0.3.2] - 2026-08-10（feature/v0.3.1 迭代）

### Added（新增）
- 工作计划展示板（PlanBoard）：从 agent 回复中的 `- [ ] / - [x]` 任务列表自动解析为可勾选的任务板（opencode 风格，带进度条）
- 推理强度选择：对话设置栏新增 ⚡快速/⚖标准/🧠深度 三档（映射 pi `setThinkingLevel`）
- 对话设置栏：新建会话/Office/创作/模型选择移至输入区下方

### Fixed（修复）
- 修复 "Agent is already processing" 报错：entry.busy 与 pi 内部 isStreaming 不同步（收尾/compaction 期）导致的竞态，加 try-catch 重试走 steer 队列
- 修复创作模式产物不显示：agent 上下文强制写入工作区（office-workspace），否则 snapshotWorkspace 检测不到

### Changed（变更）
- 批注重设计（Word 风格）：点击批注 → 定位段落 + 整段黄底高亮 + 段落旁编号徽标（锚点）+ 滚动居中；关键词提取从批注指令中匹配正文，匹配失败回退首段
- chat-head 精简：只保留标题/连接状态/当前文件提示

## [v0.3.1] - 2026-08-10（feature/v0.3.1）

### Added（新增）
- 智能体广场：4 个面向交通规划工程师的专用助手（公文报告编制/地图可视化/文本审查/数据分析可视化），一键 @ 到对话栏（角色指令 + 依赖技能）
- `AGENT.MD` 长期记忆文件：项目工作准则（命名规范/数据校验/工作流/编码准则），参考 CLAUDE.md 制定，保持简短
- Word 式批注：点击批注后，在文档段落内以黄底标记高亮匹配文本（TreeWalker 文本级定位）

### Fixed（修复）
- 修复技能按钮点击无弹窗：`useMemo` 在 `if (!open) return null` 之后调用违反 Hooks 规则，已移动到条件之前

### Changed（变更）
- 最近会话栏移到整个对话面板最顶部（chat-head 之上）
- 全局滚动条统一风格：自定义细滚动条（8px、圆角、hover 高亮），暗/亮主题用 CSS 变量自动适配

## [v0.3.0] - 2026-08-10（develop 分支）

### Added（新增）
- 统一图标库：新增 `Icon.jsx`（线性 SVG 风格），替换全部散落 emoji/字母图标（文件类型/菜单/操作/状态/头像）
- 多主题支持：暗色（默认）+ 亮色主题，顶部栏一键切换，localStorage 记忆
- 用户/Agent 头像：圆形头像，用户为蓝色系、agent 为青绿色
- 技能广场分类：按交通规划/文档报告/图表可视化/图像生成/媒体内容/Office办公/开发工具/搜索研究分类展示
- 技能集合（工作流）：预置 4 个面向交通规划工程师的组合工作流（规划报告全流程/OD数据分析/公交规划专项/汇报材料速成），一键启动
- 会话历史按文档过滤：历史标签页「当前文档」按钮，只显示与当前打开文件相关的会话
- 附件导入扩展：支持 docx/xlsx/pptx/md/pdf/txt/csv/json 附件上传（服务端保存到工作区，agent 可读取）
- 对话栏顶部最近会话栏：显示最近 5 条会话，点击直接加载
- MD/文本文件批注：从 agent 会话提取针对该文件的修改指令作为批注记录，可在文本预览右侧面板显示并定位

### Changed（变更）
- 对话气泡统一尺寸：text/thinking/tool 块统一宽度与圆角
- 上传按钮区增加「附件」按钮，发送按钮/停止按钮改用图标
- `/api/sessions` 支持 `?file=` 参数过滤
- `/api/doc/:file/comments` 对 md/txt 返回会话衍生的修订记录

## [v0.2.0] - 2026-08-10

### Added（新增）
- MD 文件目录浮窗插件：打开 .md 文件时左侧显示可折叠目录，点击标题可定位到对应段落，滚动时自动高亮当前标题
- 对话总结产物可点击：本轮对话中的文件产物（📄 标签）点击后直接在中间文件预览区打开
- 文件侧栏右键菜单：支持「在文件管理器中打开 / 复制文件名 / @到对话中 / 删除」
- 批注定位与高亮：批注面板点击后自动滚动并高亮对应段落（3 秒脉冲动画）
- 批注手动刷新按钮（🔄），修改文件后可点击重新拉取批注
- 全局错误边界：组件渲染异常时显示友好错误页，不再直接白屏
- SSE 断线自动重连机制

### Fixed（修复）
- 修复对话后页面整页刷新白屏：添加错误边界 + mountedRef 状态保护 + 竞态延迟
- 修复 MD 文件无法滚动：修正 `.mdview-container` / `.docview` 高度链与 overflow
- 修复目录浮窗无法定位：改进标题 ID 生成（对齐 rehype-slug），点击跳转改为 ID+文本双重查找
- 修复文件管理器打开失败：服务端 `/api/open-in-explorer` 改用 ESM import + `cmd /c explorer /select`

### Changed（变更）
- SSE 连接支持自动重连（断线 3 秒后重试）
- 文件修改后批注自动重新拉取（fetchComments 抽离为可复用回调）

## [v0.1.0] - 2026-07

### Added（新增）
- Office Agent Web 初版：pi agent + officecli 集成
- 对话流分块渲染（思考/工具/文本独立气泡）、流式输出、复制/耗时/折叠
- 工作区管理（默认工作区/自定义路径）、会话历史（新建/重命名/删除/加载）
- 文件多标签打开（.docx/.xlsx/.pptx/.md/.html）
- MD 渲染增强（GFM/公式 KaTeX/代码高亮/标题锚点）
- Excel 多 sheet 查看与单元格编辑
- 技能管理弹层（@调用/导入/导出 skills）
- 双模式切换（📝 Office 编辑 / 🎨 创作）
- 对话总结条（产物清单）
- 文件实时预览（watch 模式）、批注查询面板

### Fixed（修复）
- 修复 ChatPanel TDZ 错误导致前端白屏（patch 移到 scheduleFlush 之前）
- 修复 MD 滚动（删除 CSS 重复覆盖）
- 修复 XLSX 多 sheet 加载、历史过滤 pi 会话
