## [v0.8.27] - 2026-08-16（feature/v0.8.25-map-enhance）

### Fixed（修复）
- **agent 读取不到切换后工作区的文件**：agent 上下文（agentsFiles 提示词）在会话创建时用启动常量 `WORKSPACE_DIR` 写死默认工作区，用户切换工作区后 agent 仍去旧目录找文件（如「交通枢纽流量核对报告.docx」报不存在）
  - 新增 `writeContextFile`：`.agent-context.md` 改为**每次对话前刷新**，动态写入「当前工作区」绝对路径（getWorkspace()）+ 当前工作文件 + 工作区记忆摘要
  - agent 提示词改为先读 `.agent-context.md` 获取工作区/当前文件（不再假设默认路径）；context 文件路径改为相对 `.agent-context.md`（移除 Windows 硬编码绝对路径）
  - 记忆读写（readMemoryContext/writeMemorySection）跟随当前工作区
  - 服务端其余静态点跟随工作区：文件上传（/api/files/upload）、HTML 标注目录（.annotations/）、工作区 watch 快照（maps 目录）
  - officecli/map 工具原本已用 getWorkspace() 动态解析，无需改动

## [v0.8.26] - 2026-08-12（feature/v0.8.25-map-enhance）

### Added（新增）
- **OD 流量热力图**（R5）：地图顶栏「OD分析」弹窗——粘贴或上传 CSV（起点/终点经纬度 + 流量，中英文列名自动检测映射，可手动调整）→ 起点流量加权热力渲染（蓝→红渐变，流量归一化权重，缩放自适应半径）+ 可选 OD 流向线（流量分级着色/粗细）；统计信息（OD 数/总流量/峰值）；临时图层可一键清除
- **路径规划**（R6，开源）：地图分析弹窗「路径规划」tab——地图选起点/终点 + 驾车/步行/骑行，默认开源 OSRM（零配置），可选高德 provider（AMAP_KEY）；路径线 + 红绿起终点标记 + 距离/时间信息条；服务端 /api/map/route 可插拔（OSRM_URL 可指自托管）

### Changed（变更）
- 等时圈弹窗升级为「地图分析」弹窗（等时圈 / 路径规划 tab 切换）

## [v0.8.25] - 2026-08-12（feature/v0.8.25-map-enhance）
## [v0.8.25] - 2026-08-12（feature/v0.8.25-map-enhance）

### Added（新增）
- **地图标注渲染**（R2）：样式编辑器新增「标注」区块——显示开关/字段选择（自动探测 name/ref 等）/字号/颜色，为图层生成 `{layerId}-label` symbol 层（text-field），关闭即移除；持久化 style.json，agent 也可 map_edit 控制
- **模板一键生成**（R4）：模版库预览新增「用此模板生成」按钮——输入主题 → 返回对话自动预填指令（`@模板[名称] 请参考风格生成主题为「xx」的 HTML PPT`），agent 按模板风格生成
- **报告图导出**（R3）：顶栏「导出」改弹窗——A4 横向/纵向/方形/自定义尺寸 + 标题 + 图例开关；输出高分辨率 PNG（150-300dpi）合成：地图 + 标题条 + 比例尺（按缩放级别自动取整）+ 指北针 + 图层图例
- **路网批量导入**（R1）：顶栏「导入数据」弹窗两方式——① 批量文件（多选 GeoJSON，文件名关键词自动归类）② 目录一键生成（工作区目录 → prepare-map-data + 瓦片重建）；服务端新增 /api/map/import-batch、/api/map/prepare

- **路径规划（开源 OSRM）**：地图分析弹窗新增「路径规划」tab——选起点/终点（地图点选）+ 驾车/步行/骑行，默认走开源 OSRM（零配置，基于 OpenStreetMap），可选高德 provider（AMAP_KEY，中国路网更准）；结果绘制路径线+起终点标记+距离/时间信息条；服务端 /api/map/route（可插拔 provider，OSRM_URL 可指自托管）

### Fixed（修复）
- 无

## [v0.8.24] - 2026-08-12（develop）
## [v0.8.24] - 2026-08-12（develop）

### Fixed（修复）
- **知识库无法与 agent 关联（@知识库 引用失效）**：kb.json 根目录多为 Windows 路径（F:\...），Mac 上无效导致索引为空——已添加本机真实知识库目录（/Volumes/移动固态硬盘/工作文件夹/文档，8 篇交通规划 md），kb_read 实测可读全文（16048 字）；前端知识库根目录下拉对无效根显示「（缺失）」标记

### Changed（变更）
- **知识图谱布局对齐 SiYuan**：节点尺寸改为对数缩放（log2(度+1)+0.6）×12（上限 56），大度节点不再尺寸爆炸；初始布局由随机分散改为**黄金角螺旋**（角度=index×黄金角+id 哈希扰动，半径=√(index+1)×间距），初始化更均匀不堆叠
- **文件树展示优化**：文件行右侧显示文件大小（B/KB/MB）与修改时间（相对时间），目录行显示 ▶ 指示，名称超长省略号

## [v0.8.23] - 2026-08-14（本地：参考 siyuan 方案的知识库图谱增强）

### Added（新增）
- **图谱 minRefs 过滤滑块**（siyuan 式）：控制条新增「最少引用」滑块（0-8），引用数低于阈值的文档节点隐藏（局部图谱聚焦节点保留）
- **图谱按类型过滤**（siyuan 式）：文档/标签/目录三个独立开关
- **图谱配置持久化**：过滤参数保存到 localStorage（oaw_graph_settings），刷新不丢
- **文档底部反链区块**（siyuan 式）：正文下方显示「反向链接 + 提及」列表（来源文档标题 + 引用上下文摘要，点击跳转），与左栏信息区反链互补
- **引用悬浮预览**（siyuan 式）：知识库文档中的 [[双向链接]] 渲染为虚线链接，hover/点击 → 读取目标文档弹窗预览（标题 + 内容摘要，400ms 防抖 + 多候选路径解析）

### Changed（变更）
- MarkdownBody 支持 onWikilinkHover 可选回调（聊天消息不受影响）


## [v0.8.22] - 2026-08-13（本地：模板库/知识库 @ 重构 + HTML 预览修复 + 历史栏移到对话侧）

### Added（新增）
- **预览面板 @ 到对话**：模版库卡片上的 @ / @📁 按钮移除，改为在预览面板中「@ 模板」/「@📁 目录」按钮
- **知识库文件夹 @**：文件树目录行新增 @ 按钮（`@知识库目录[路径@根目录]`），agent 读取目录下所有文档
- **多次 @ 累积**：@ 不再立即退出并跳转，可连续 @ 多个模板/文档/文件夹（顶栏显示「@已选 N 项」），点返回时统一插入对话输入框

### Fixed（修复）
- **OpenDesign HTML 预览 file not found**：模板扫描 relPath 为绝对路径（反斜杠）导致文件路由解析失败；归一化为相对项目根 + 正斜杠，文件/内容接口双根（项目 templates/ 与工作目录 _报告模板/）解析

### Changed（变更）
- **会话历史移到对话栏一侧**：左侧栏只保留文件树；对话面板顶部新增可折叠「历史」抽屉（默认隐藏，点击展开，选择会话后自动收起）
- 地图模式（整页 GIS）对话栏同步支持历史抽屉与多次 @


## [v0.8.21] - 2026-08-13（本地：ask_user 主动询问 + 模板库增强 + 会话标题 + 工具激活修复）

### Added（新增）
- **ask_user 主动询问**：agent 遇到任务要求不明确（文档类型/格式/篇幅/受众/数据来源等）时主动调用 ask_user 工具提问，前端对话流显示「需要你确认」卡片（问题 + 快捷选项 + 输入框），用户回答后 agent 继续执行；5 分钟未回答自动按专业判断继续
- **模板库 @文件夹**：模板卡片新增「@📁」按钮，插入 @模板目录[相对路径] 标记，agent 读取整个目录下所有模板文件
- **HTML 模板过滤**：OpenDesign 分类只收录 example.html（106 个有效模板），排除集合页/子页面等占位 html

### Fixed（修复）
- **关键：全部自定义工具未激活**（pi SDK 升级后仅激活 tools 白名单工具，kb_search/map_read/memory_update/ask_user 对模型不可见）——tools 白名单补齐 + setActiveToolsByName 显式激活，知识库/地图/记忆/询问工具全部恢复可用
- **会话标题被系统提示污染**：跳过前端注入的 [当前工作文件]/[模式]/"优先用 officecli..." 提示消息，取第一条真实用户指令

## [v0.8.20] - 2026-08-13（本地 UI 修复）

### Fixed（修复）
- **自定义工作区切换后不出现在下拉列表**：handleWorkspaceChange 切换后把新路径加入 workspaces 列表（此前下拉保持旧列表，用户找不到已切换的工作区）
- **文件树无返回上一级按钮**：进入多层子目录时 crumb-bar 新增「↑ 上一级」按钮（返回父目录），与「← 根目录」并存

### Removed（移除）
- 文件树底部冗余的 office-workspace 页脚文本
- 底部「产物与设置点击后弹窗显示」提示文本


## [v0.8.19] - 2026-08-12（本地修复 + 稳定性 + 模板库 @ 到对话）

### Added（新增）
- **模板库 @到对话**：模版库每个模板（网格/列表视图）新增「@」按钮，点击退出模版库并把 `@模板[文件名]` 标记插入对话输入框；agent 识别标记后用 find+read 读取对应模板作为撰写参考（规划素材库/OpenDesign/报告模板全覆盖）

### Fixed（修复）
- **地图模式崩溃**：App.jsx 的 MapPanel import 在远程合并时丢失（`MapPanel is not defined` 白屏），已补回
- **agent 模板库联通**：agent 上下文补充规划素材库（templates/traffic-material 14 份模板）说明与 @模板 标记识别指引
- **界面恢复脏数据**：localStorage 中残留非法文件名（URL 编码/正则片段）导致恢复时 404，恢复逻辑增加文件名合法性过滤

### Changed（变更）
- **加载反馈**：页面加载中显示品牌占位（Logo + "正在加载…"），消除 React 挂载前的无反馈白屏
- **验证脚本**：API 冒烟测试服务器等待超时 20s → 60s（服务器初始化较慢时不再误报失败）

## [v0.8.18] - 2026-08-12（main）

### Added（新增）
- **规划素材库分类**：内置 14 份交通规划领域详版模板（templates/traffic-material/，来自《07模板案例库/_模板素材库》同步固化）：年度工作报告/五年发展规划/规划文本条文式/工程可研/线位论证预可/选址用地预审/交通影响评价/汇报材料/物流园区规划/规划研究报告/PPT 汇报/总览通用规范/格式数据汇总/素材库深挖；标题自动提取文件内真实标题（去除序号前缀），支持预览/搜索/生成

## [v0.8.17] - 2026-08-12（feature/0.8.13-map-align）

### Added（新增）
- **统一命令面板（Ctrl/Cmd+K）**：跨模块搜索（📄文件 / 📚知识库 / 📋模板 / 🗺️地图项目 / 💬会话），输入即过滤 + 知识库实时搜索，↑↓/Enter/Esc 键盘导航，回车直达（打开文件/进入对应全屏模式）
- **HTML 标注模式**：打开 .html 文件工具栏「标注」按钮 → 划词浮动工具条（🖍高亮 / 💬批注 / 🤖交给 agent）；高亮 <mark> 包裹、批注下划线带备注；标注持久化到工作区 .annotations/（防抖自动保存），重新打开自动恢复；「交给 agent」把选中内容+指令送入对话（officecli/agent 定向修改入口）
- **文档 ↔ 知识库联动**：
  - 文件区右键 docx「存入知识库」→ 服务端解析 docx 提取文本（含标题层级）转 Markdown，工作区自动注册为知识库根
  - 知识库文档标题栏「导出 Word」→ Markdown 转 docx（标题层级/段落）写入工作区，文件名自动查重
- **服务端**：POST /api/kb/ingest（docx→md 摄取）、POST /api/kb/export-docx（md→docx 导出）、GET/POST /api/doc/:name/annotations（标注持久化）；server 新增 jszip 依赖（docx 解析）

### Fixed（修复）
- **命令面板首次打开白屏**：CommandPalette 早退 `if (!open) return null` 前有 useMemo（indexMap），open 切换导致 hooks 数量变化（React #310）——改为普通计算

## [v0.8.16] - 2026-08-12（feature/0.8.13-map-align）

### Added（新增）
- **红头会议通知生成器**：模版库顶栏「生成红头通知」按钮 → 表单填写公文要素（发文机关/标题/文号/主送/引言/通知事项/落款/日期）→ 服务端 docx 库按 GB/T 9704 规范生成（红色版头+标题·方正小标宋·红线·1.5倍行距·首行缩进）→ 自动保存到工作区（文件名查重）
- **server/notice.mjs**：红头生成核心模块（页边距上3.7/下3.5/左2.8/右2.6cm 近似 twips），供前端路由与后续 agent 工具复用
- **server 依赖**：新增 docx 库（服务端文档生成能力）
- **文件区新建空白 Word 文档**：文件区「+」按钮，docx 库生成空白文档上传工作区（自动查重命名：新建文档.docx / 新建文档 2.docx…），为 agent/officecli 内容生成提供入口

## [v0.8.15] - 2026-08-12（feature/0.8.13-map-align）

### Added（新增）
- **OpenDesign HTML PPT 模板库**（nexu-io/open-design，GitHub 开源）：
  - 调研确认 114 个模板目录（含 64 款 html-ppt-* 系列：zhangzara 各风格/tech-sharing/pitch-deck/product-launch/weekly-report/xhs 等）
  - 全部下载固化到 `templates/opendesign/`（29MB，1189 文件，含 html-ppt skill 包引擎 templates/assets），提交进仓库
  - 模板库新增「HTML PPT（OpenDesign）」分类（157 个 HTML 模板：106 个 example 首页 + html-ppt 引擎变体），自动过滤 assets/themes/animations 引擎资源
  - HTML 模板预览改为 **iframe 直接渲染首页**（新增 /api/templates/file 文件流路由，相对资源正确解析），附「新窗口打开」链接
- 修复：tpl.mjs 绝对路径分类目录扫描（path.isAbsolute）、getTemplateContent 路径解析

## [v0.8.14] - 2026-08-12（feature/0.8.13-map-align）

### Fixed（修复）
- **Word 图片不显示（空白框）**：docx-preview 的 `useBase64URL` 模式下图片 src 经 FileReader 异步链路赋值为空——改用 Blob URL（同步 createObjectURL），实测 400×240 大图正常渲染
- **知识图谱初始化再次堆叠**：力导向布局参数整体按密度可调（见 Added），默认增强 linkDistance/nodeStrength/collide 半径

### Added（新增）
- **消息目录珠子时间线（pi web 风格）**：左侧竖轨 + 用户消息圆珠（按消息在对话流中的位置比例分布），agent 回答不占珠子；悬停珠子显示问题摘要、点击定位
- **目录导航可缩放**：Word 目录栏宽度可拖拽（160-380px），拖动右侧手柄调整
- **历史/文件垂直分割**：侧栏历史与会话文件区之间可上下拖动调整高度
- **三栏布局水平调整**：地图（左右栏）、知识库（左右栏）、模版库（分类栏/预览栏）均支持左右拖动调整宽度
- **图谱布局密度控制**：工具栏「间距」滑杆（0.6×紧凑 ~ 1.8×舒展），实时重建力导向布局
- **会话卡片式分隔**：历史会话项改为独立卡片（间距/圆角/边框），悬停高亮，不再紧贴

### Changed（变更）
- 会话操作按钮在卡片内 hover 显示（置顶/重命名/删除图标）

## [v0.8.13] - 2026-08-12（feature/0.8.13-map-align）

### Added（新增）
- **侧栏产物/设置弹窗化**：底部「产物」「设置」按钮点击后弹窗显示（设置弹窗含「设置/记忆」子 tab，记忆整合进设置）；侧栏底部不再内嵌面板
- **文件树搜索框**：文件区顶部搜索框实时过滤文件
- **皮肤系统**：设置中可选 5 套皮肤（默认青绿/海洋蓝/森林绿/暖橙/紫罗兰），通过 data-skin CSS 变量覆盖强调色，暗/亮主题独立适配，localStorage 持久化
- **模型 API Key 配置**：设置中可添加/删除 Anthropic/OpenAI/Gemini/DeepSeek/Moonshot/Qwen 等 provider 的 API Key，写入 pi 原生 auth.json 并注入运行时（setRuntimeApiKey），列表掩码显示；服务端 /api/agent/auth 增删查接口
- **知识图谱优化**：节点初始随机分散 + 力导向参数增强（linkDistance 220/330、nodeStrength -1200、collide 碰撞半径），避免初始化全部堆在中心；节点/边动画（enter/update 400ms ease-out）更丝滑；方格纸背景按主题适配（暗色白格线/亮色灰蓝格线+纸色底）；标签/高亮颜色跟随主题 CSS 变量，暗色下对比度提升

### Fixed（修复）
- **Word 目录导航失效**：docx-preview 实际生成的标题类名为小写 `oaw-docx_heading1`，目录提取选择器匹配大写 `Heading` 永远落空——改为大小写不敏感匹配
- **修订/批注开关无效**：renderAsync 选项写死强制开启，工具栏按钮点击无效——改为跟随开关状态（默认开启修订与批注）
- **历史会话拥挤**：置顶/重命名/删除操作改为 hover 显示图标按钮，会话列表禁用横向滚动

### Changed（变更）
- 侧栏底部三 tab（产物/记忆/设置）→ 两按钮（产物/设置）+ 弹窗；记忆并入设置弹窗
- 远程 0.8.x 与本地 0.7.x 地图功能对齐合并（feature/0.8.13-map-align）：地图三栏全屏 + 品牌 Open Plan + 记忆机制 + MindMap 等全部保留

## [v0.8.12] - 2026-08-12（feature/v0.8.12-branding）

### Changed（变更）
- **项目更名为 Open Plan（规聚）**：规划 + 聚集，面向交通规划工程师的规划工作台
  - package.json / package-lock.json name → open-plan
  - 页面标题 → Open Plan 规聚；favicon → 品牌 Logo
  - 顶栏 / 对话面板头部显示品牌名 + Logo
  - README 更新项目名与简介
  - server 启动日志、agent 上下文标题同步更新（会话过滤逻辑中的目录判断保持不变）

### Added（新增）
- **品牌 Logo（Logo.jsx）**：圆角方块渐变底 + 规划网格线 + 四角汇聚节点 + 中心聚焦圆——"规"（网格/规划）与"聚"（节点汇聚）的视觉融合，跟随主题 accent 色系，支持任意尺寸

## [v0.8.11] - 2026-08-12（feature/v0.8.11-memory）

### Added（新增）
- **工作区记忆接入 agent 上下文**：每次对话前自动读取 `office-workspace/AGENTS.md` + `memory/*.md`（合并限长 4000 字）注入 agent 上下文，agent 任务开始前即可感知项目准则/用户偏好/经验教训
- **memory_update 工具**：agent 完成任务后自主评估是否沉淀长期记忆，按 section（项目信息/用户偏好/经验教训）写入 `memory/MEMORY.md`（替换同 section、单条 ≤100 字、总长控制）
- prompt 每次调用前刷新 agent 上下文（记忆/当前文件变更即时生效）

### Fixed（修复）
- 记忆文件此前仅前端 MemoryTab 手动编辑，agent 完全不可见；且 agent 尝试更新记忆时写错路径（项目根目录）——上下文已明确记忆文件路径与工具引导

### 备注
- 记忆保持简短（单条 100 字内、总长 6000 字内），符合"内容和行为准则不宜太长"的要求

## [v0.8.10] - 2026-08-12（feature/v0.8.10-session-title）

### Fixed（修复）
- **会话标题被前缀污染**：标题此前为第一条用户消息原文，包含前端注入的 `[当前打开文件: xxx]`、`[模式: 办公/创作]`、`[已上传附件: ...]` 标记，导致历史列表"只显示工作名"。新增 `cleanSessionTitle` 清洗函数：
  - 去除全部 `[...]` 注入前缀标记，取真实指令内容
  - 超过 50 字时按句子边界（。！？）智能截断，不再硬切
  - 首条用户消息清洗后为空时回退 label

### 备注
- LLM 摘要自动命名（4-12 字标题）列为后续可选项：pi SDK 的 `ModelRuntime.completeSimple` 需构造 pi Context 对象，成本较高，暂缓

## [v0.8.9] - 2026-08-12（feature/v0.8.9-chat-input）

### Changed（变更）
- **输入区重排为单行工具栏**（参照 pi-web ChatInput 底栏）：
  - 输入行：textarea（flex）+ 右侧发送/停止按钮
  - 工具栏行：图片导入按钮 → 附件按钮 → 分隔 → 模式切换（📝办公 / 🎨开发，图标化）→ 分隔 → 模型图标 → 思考程度图标 → spacer → 新建会话（+）
- **模型选择图标化**：长下拉改为机器人图标 + 点击弹出浮层（搜索过滤 + [V] 视觉标记 + 当前模型圆点指示），90+ 模型不再挤占工具栏
- **思考程度图标化**：下拉改为图标 + 点击弹出三档（快速/标准/深度，带描述）

### Removed（移除）
- 原 model-picker / effort-picker 长下拉（select），chat-settings 区并入单行工具栏

## [v0.8.8] - 2026-08-12（feature/v0.8.8-sidebar-settings）

### Changed（变更）
- **左侧栏重排**（上历史 / 中文件树 / 下产物记忆设置）：
  - 顶部：工作区选择器 + 新建会话按钮（+ 图标）
  - 中部：会话历史折叠区（可折叠、搜索、按当前文档过滤、置顶/重命名/删除）+ 文件树区（面包屑/上传/刷新/右键菜单）
  - 底部：产物 / 记忆 / 设置 三个 tab（产物按修改时间倒序；记忆为 AGENTS.md + memory/ 编辑；设置为新设置面板）

### Added（新增）
- **设置面板**（SettingsPanel，左侧栏底部「设置」tab，localStorage 持久化）：
  - 外观：主题切换（暗/亮）、消息字体大小（小/中/大 → CSS 变量即时生效）、批注高亮时长（滑块 5~60s，CommentMarker 实时读取）
  - 对话行为：思考块默认展开开关（ThinkingBlock 读取）、消息目录栏显示开关（ChatTimeline 读取）
  - 模型 / 服务集成（officecli 状态实时检测、高德/IMA 配置引导）/ 高级（重置界面状态与设置、版本号）
- Icon 新增 gear/book 图标

## [v0.8.7] - 2026-08-12（feature/v0.8.7-comment-restore-vision）

### Fixed（修复）
- **批注内容截断**：`.comment-marker-text` 的 `-webkit-line-clamp: 3` 导致批注只显示 3 行，移除截断，完整显示（保留换行/长文本自动换行）
- **批注高亮时间太短**：黄底高亮 + 编号徽标从 5 秒延长到 20 秒
- **对话刷新后丢失（重要）**：三层根因修复——
  1. `/api/agent/prompt` 响应新增 `sessionId`（pi 会话 id），前端上报 App 持久化（原链路完全缺失，App 不追踪活跃会话）
  2. `/api/sessions` 会话 id 字段名与 pi SDK header 不一致（`h.id` vs `h.sessionId`）导致新会话永不匹配；`findSessionFile` 同步修复
  3. 新会话 cwd 为 pi 会话存储目录（`.pi/agent/sessions`），被 isOaw 过滤误杀，加入白名单
  4. 刷新后保存逻辑先于会话恢复执行，把 lastSessionId 覆盖为 null 的竞态——用 ref 缓存旧值，恢复完成前不覆盖

### Changed（变更）
- **视觉对比度**：用户消息高对比强调（accent 左边框 + 文本加粗），agent 回复文字弱化（muted 色），重点突出"我的任务"

## [v0.8.6] - 2026-08-12（feature/v0.8.6-chat-flow-piweb，参照 pi-web 重构对话流）

### Changed（变更）
- **对话流重构为 pi-web BlockView 模型**：移除「执行过程」大分组框（ProcessBlockGroup），思考/工具调用/最终文本按原始顺序**各自独立成条目**垂直排列（`msg-blocks`），宽度统一撑满消息列
  - 最终回复文本：**去掉气泡边框**（flow-markdown 无背景无边框），宽度与事件流条目一致
  - 思考块：独立灰色卡片（圆角），默认折叠成一行（思考 N s），点击展开全文
  - 工具调用：独立绿色边框卡片（成功/失败/进行中三态着色），头部 = 工具名 + 参数预览 + 耗时，点击展开输入/输出
- **消息目录栏固定**：ChatTimeline 从消息滚动区移到聊天面板层（chat-head 下方左侧），滚动消息时不再跟随消失；悬停展开/点击定位功能不变

### Removed（移除）
- ProcessBlockGroup 组件与相关 CSS（.process-group/.process-head/.process-body、.markdown-bubble）

## [v0.8.5] - 2026-08-12（feature/v0.8.5-mindmap）

### Added（新增）
- **知识库脑图视图**：知识库新增「脑图」视图（浏览/思维图谱/脑图三态切换）
  - 左栏大纲（目录树缩进列表，目录可展开/折叠）+ 中栏 G6 树状脑图（indented 布局，目录/文档节点分级着色）+ 右栏文档预览
  - **双向联动**：点击大纲文件 → 脑图高亮 + 右侧预览文档；点击脑图节点 → 大纲高亮 + 预览文档
  - 节点 id 用完整路径（不同父目录下同名目录不冲突）

## [v0.8.4] - 2026-08-12（feature/v0.8.4-merge-map-panel，合并自 feature/0.7.5-map-panel）

### Added（新增）
- **QGIS 风格图层面板（LayerPanel，移植自 v0.7.5）**：图层分组（基础地理/路网/设施点）、显隐勾选、拖拽排序、符号缩略图（SVG 图例）、右键菜单（属性表/缩放到图层/重命名/复制/删除）、样式编辑器（颜色/线宽/透明度/虚线/按字段渲染调色板）
- **属性表（AttributeTable）**：图层要素表格，字段/搜索/排序/分页/行定位/CSV 导出，地图上定位要素
- **测量/绘制工具**：测量距离（haversine）、测量面积（多边形近似）、标绘点；地图点击加点、双击完成、Esc 取消
- **等时圈分析**：工具箱入口，地图点击选起点 → 高德 Web 服务 v5 isochrone（需 AMAP_KEY）→ 多边形叠加显示，支持清除
- **Agent 地图工具（map_read / map_edit / map_import）**：agent 可结构化读取地图状态、编辑样式（显隐/颜色/线宽/顺序/新增图层）、导入 GeoJSON 图层（自动建瓦片），前端实时刷新
- **服务端 API**：GET/DELETE /api/map/project/:name/layer/:layerId、POST /api/map/project/:name/isochrone

### Changed（变更）
- **知识库图谱升级（siyuan 风格，移植自 v0.7.5）**：分组调色（Obsidian 式）、hover 聚焦（邻接高亮非邻接降透明）、节点大小∝连接度、缩放隐藏标签、搜索定位、隐藏孤立节点、大数据分批渲染；边类型可选（双向链接/内容相似/标签/目录）；局部图谱（1-hop 聚焦当前文档）
- **知识库浏览增强**：文件树懒加载+展开持久化、反链上下文、标签交互、文档标题栏面包屑、提及检测（mentions）
- MapPanel 工具箱扩充至 10 项工具；图层 tab 升级为 LayerPanel
- MapViewer 新增：setLayerPaint/zoomToLayer/moveLayerTo/startDraw/stopDraw/showIsochrones/pickLocation 外部方法

### Fixed（修复）
- 服务器新增地图路由后需重启生效（旧进程加载旧 index.mjs）

## [v0.8.3] - 2026-08-12（feature/v0.8.3-chat-redesign）

### Added（新增）
- **会话消息目录栏（ChatTimeline）**：对话区左侧窄条，鼠标悬停展开消息目录、移开隐藏；每条消息一个条目（角色图标+摘要+状态），点击滚动定位到对应消息；随流式输出实时更新（进行中条目呼吸点）
- **超大消息保护**：AI 回复 >100KB 时不再直接跑 markdown 渲染（防浏览器卡死），显示"点击查看全文"按钮，展开后纯文本预览

### Changed（变更）
- **气泡精简**：思考块默认折叠成一行（"思考 N s"），点击才展开，流式"思考中…"不再占用大段空间
- **相邻消息头部精简**：连续同角色消息（如连续 AI 回复/连续用户消息）省略重复的作者+时间头部

### Fixed（修复）
- **历史会话恢复导致对话面板崩溃**：ToolCard 对 `JSON.stringify(undefined)`（历史会话中 tool block 缺失 input）返回 undefined 未防护，`.slice` 抛错——补空串兜底

## [v0.8.2] - 2026-08-12（feature/v0.8.2-persist-ui）

### Added（新增）
- **界面状态固化**：刷新/重开浏览器后恢复到上次界面——打开的文档 tabs（重新加载内容）、激活 tab、全屏模式（知识库/模版库/地图）、当前工作区、子目录、侧栏开关、最后会话
- 新增 `client/src/persist-ui.js`：localStorage 状态存取（`oaw_ui_state_v1`）

### Changed（变更）
- App.jsx 记录当前会话 id；恢复完成前不写入状态，避免启动瞬间覆盖已保存界面

## [v0.8.1] - 2026-08-12（feature/v0.8.1-comment-locate）

### Fixed（修复）
- **Word 批注无法定位（困扰已久）**：DocxViewer 批注列表点击调用未定义的 `handleCommentClick`（ReferenceError），现改用 CommentMarker 组件统一渲染批注列表
- **批注精确定位**：利用 docx-preview 渲染的批注锚点（HTML 注释节点 `start of comment #N`）按文档顺序对应批注索引，滚动定位到批注段落 + 黄底高亮 + 编号徽标；关键词匹配降级为兜底
- **移除"取文档第一段"的粗暴回退**（匹配失败时不再错误定位到开头）
- **修复 DIV 容器下定位失效**：`createNodeIterator`/`createElement` 是 Document 的方法，docx 渲染容器是 div 时原实现静默失败（异常被 try/catch 吞掉），现统一走 `ownerDocument`
- 删除废弃的 `.oaw-docx-comments` 列表样式

### Changed（变更）
- DocxViewer 批注面板接入 CommentMarker（containerRef=docx 渲染宿主），与 md/html 预览共用同一套定位组件

## [v0.8.0] - 2026-08-11（feature/map-visualization）

### Added（新增）
- **地图可视化模式**：顶栏新增「地图」按钮，与技能/智能体/知识库/模版库并列。全屏模式布局：左侧 QGIS 风格面板（文件/工具箱/图层三 tab）+ 中央 MapLibre GL 地图 + 右侧复用现有对话栏
- **浙江省交通基础图层**：11 地市边界 + 90 区县边界（DataV GeoAtlas 下载）、高速公路 28,526 段、国省道/快速路 19,546 段（OSM 数据含道路名/编号/车道/限速/流量）、收费站 404 个、枢纽 769 个
- **矢量瓦片引擎**：geojson-vt + vt-pbf 纯 Node 生成 z/x/y.pbf 瓦片（脚本 scripts/build-vector-tiles.mjs，服务端 /api/map/data/{project}/tiles/ 按需加载），40MB 原始数据 → 19MB 瓦片按缩放级别切片
- **开源底图**：Carto 亮色（默认）/ OSM / Carto 暗色 / Esri 卫星影像四选一，切换只改图层显隐不重载样式
- **图层管理**：显隐勾选、透明度滑块、上下排序、底图切换、点击要素弹窗查看属性（名称/编号/流量等）
- **数据导入**：地图左上角「导入」按钮支持 .geojson / .json，以及 .shp+.dbf(+.prj)（shpjs 浏览器解析），导入后自动建瓦片并加入图层面板
- **导出图片**：工具箱支持导出 PNG（1x）与高清导出（3x 像素比），MapLibre 原生 canvas 截图
- **Agent 对话改图**：对话可修改 style.json（颜色/线宽/显隐）与 layers/*.geojson（数据），后端 watch 工作区 maps/ 子目录变化，SSE file_changed 触发地图 3 秒内热更新；agent 上下文已注入地图项目说明
- **后端 API**：/api/map/projects、/api/map/project/:name、/api/map/project/:name/style|config|import|rebuild-tiles，静态瓦片 /api/map/data/*
- **数据准备脚本**：scripts/fetch-boundaries.mjs（下载浙江边界）、scripts/prepare-map-data.mjs（裁剪简化路网）、scripts/build-vector-tiles.mjs（生成瓦片，支持 --layer= 单层重建）

### Changed（变更）
- server/index.mjs 工作区快照改为递归（maps/ 子目录文件变化可被检测）
## [v0.7.6] - 2026-08-11（feature/0.7.5-map-panel，开发中）

### Added（新增）
- **测量工具**：顶栏「测量」菜单（距离/面积），点击地图加点实时计算（Haversine 距离 / 纬度修正平面面积），双击完成，结果浮层显示，Esc 取消
- **绘制工具**：顶栏「绘制」菜单（点/线/面），绘制完成后命名保存为新图层（自动重建瓦片、按类型分组），支持 Esc 取消
- **按字段渲染**（样式编辑器扩展）：单一颜色 / 按字段分类（match 表达式，唯一值≤8+其他）/ 按数值分级（step 表达式，3-6 级）；字段自动探测（分级模式只列数值字段）、4 套预置调色板、图例色块预览，一键应用/恢复
- **等时圈多档对比**：勾选「多档对比」后同时计算多档时间（如 30/60/90 分钟），深→浅紫色叠加显示
- MapViewer 新增 setDrawingMode：绘制/测量模式下禁用属性弹窗、光标变十字

### Changed（变更）
- 绘制/测量使用临时地图图层（draw-temp / iso-temp*），不写入项目，导出/重建瓦片不受影响

## [v0.7.5] - 2026-08-11（feature/0.7.5-map-panel，开发中）

### Added（新增）
- **QGIS 风格图层面板**（LayerPanel 重构）：
  - 图层分组：基础地理 / 路网 / 设施点（config.layers 新增 group 字段，导入图层按类型自动归类），组头可折叠、整组显隐开关、图层计数
  - 拖拽排序：HTML5 原生 DnD 跨组/组内调整叠放顺序，实时写入 style.json
  - 图例符号缩略图：从图层 paint 属性自动推断线/点/面 SVG 小图标（颜色/线宽/半径实时反映样式）
  - 右键菜单：属性表 / 缩放到图层 / 重命名（行内双击也可编辑）/ 复制图层 / 删除图层
  - 图例信息展开：要素数、属性字段列表、样式摘要（颜色/线宽/透明度）
  - 样式编辑器：选中图层后底部展开，分类型控件（线：颜色/线宽/透明度/线型预设；点：颜色/半径/描边/透明度；面：填充色/边框/透明度），修改即保存 style.json 并热更新地图
- **属性表弹窗**（AttributeTable）：字段表格（列点击排序）、属性值搜索过滤、行点击定位到地图（fitBounds）、分页（每页 100）、导出 CSV（含 BOM）

### Changed（变更）
- MapPanel 左栏由扁平列表升级为 LayerPanel；旧图层行操作（上下移按钮/内联滑杆）移除，由拖拽+样式编辑器替代
- LAYER_DEFS 增加 group 字段；map.mjs defaultConfig / importLayer 自动填充分组

## [v0.7.4] - 2026-08-11（feature/0.7.4-map-gis，开发中）

### Added（新增）
- **地图全屏模式（GIS 项目）**：与知识库/模版库同款全屏布局（顶部栏「地图」按钮进入），三栏结构——左侧图层文件树、中间地图（MapLibre GL 矢量瓦片）、右侧 agent 对话栏
  - 图层文件树：显隐开关、透明度滑杆、上移/下移叠放顺序、缩放到图层、删除图层（含瓦片）
  - 顶栏工具栏：项目切换、开源底图切换（Carto 亮/暗、OSM、Esri 卫星）、重建瓦片、导出高清 PNG
  - 地图主区：要素点击属性弹窗、坐标/缩放状态栏、GeoJSON/SHP 导入（shpjs 解析，自动生成瓦片）
  - 等时圈分析弹窗：出行方式（驾车/步行/骑行/公交）+ 时间范围 + 地图选点，调用高德 v5 isochrone（服务端代理，需配置环境变量 AMAP_KEY），结果以临时紫色多边形叠加显示
- **矢量瓦片数据管线**：`scripts/fetch-boundaries.mjs`（DataV GeoAtlas 拉取浙江省 11 地市 + 90 区县边界）、`scripts/prepare-map-data.mjs`（按文件名关键词自动匹配高速公路/国省道/农村公路/收费站/枢纽，兼容 OSM 标签混合路网单文件）、`scripts/build-vector-tiles.mjs`（geojson-vt + vt-pbf 生成 pbf 瓦片）
- **农村公路图层**：LAYER_DEFS 新增 roads-rural（绿色线样式，z7-14）
- **地图服务端 API**：/api/map/projects、/project、/style、/config、/import（GeoJSON→瓦片）、/rebuild、/layer（读取/删除）、/isochrone，静态挂载 /api/map/data/*（style.json/瓦片/图层数据）
- **Agent 地图工具**：`map_read`（读项目配置/图层/样式）、`map_edit`（改图层显隐/颜色/线宽/透明度/顺序/新增样式图层，实时反映到前端地图）、`map_import`（工作区 GeoJSON 导入为新图层）；agent 上下文注入地图项目说明
- 浙江边界默认数据已就绪：boundary-city（142 瓦片）+ boundary-county（1392 瓦片）

### Fixed（修复）
- **应用白屏修复**：pptxviewjs 依赖的 chart.js（可选 peer）缺失导致 vite dev 构建失败/生产包运行时 `undefined.from` 崩溃，补装 chart.js
- **shpjs 浏览器兼容**：immediate 包引用全局 `global`、safe-buffer 依赖 Node 内置 buffer——vite 配置 `define: { global: "globalThis" }` + buffer polyfill alias
- **图层清单误列 macOS 垃圾文件**：map.mjs 过滤 `._*` 前缀文件
- **等时圈选点被弹窗遮挡**：选点模式下 backdrop 置为 pointer-events: none，允许点击地图选中心点

### Changed（变更）
- 前端依赖新增：maplibre-gl、shpjs、chart.js、buffer；服务端新增：geojson-vt、vt-pbf

## [v0.7.3] - 2026-08-11（feature/0.7.x-workspace-fix）

### Added（新增）
- **新建空白 Word 文档**：文件区工具栏「+」按钮，docx 库生成空白文档上传工作区（文件名自动查重：新建文档.docx / 新建文档 2.docx…），支持随后用 officecli/agent 按规范生成内容：server/kb.mjs 新增 `getTreeLevel`（按路径返回直接子级），前端按需展开，目录/文件行带计数徽标（子项数/引用数），hover 行内 @ 到对话操作，排序切换（按名称/修改时间）
- **知识库展开状态持久化**：文件树展开目录状态存入 localStorage（按根目录分 key），切换根目录自动恢复；打开文档自动展开祖先链并滚动定位（对齐 siyuan selectItem/setCurrent）
- **反链上下文预览**：server/kb.mjs `getDoc` 反链附带引用段落上下文（大小写不敏感匹配），右栏反向链接可点击展开/收起引用上下文
- **标签交互**：笔记内 #tag 下划线样式可点击搜索（MarkdownBody 新增可选 onTagClick，对齐 siyuan span[data-type=tag]）；文档信息区/搜索结果标签可点击搜索；图谱标签节点点击 → 执行搜索（对齐 siyuan）
- **图谱控制条**：搜索节点定位输入框 + 隐藏孤立节点开关（对齐 siyuan pruneUnref）

### Fixed（修复）
- **工作区列表混入 macOS AppleDouble 垃圾文件**：server/workspace.mjs `listWorkspace` 增加 `.` 前缀过滤（`._xxx` 元数据文件 / `.DS_Store`），文件树与产物面板不再显示，避免误点打开二进制乱码
- **产物面板打开即白屏**：client/src/components/SessionSidebar.jsx 产物 tab 调用了未定义的 `formatRelTime`，补充实现（复用 formatTime 相对时间逻辑），产物列表恢复正常显示
- **知识库根目录切换竞态**：loadRoot 清空 treeRoot 时反复触发初始加载 effect，导致切换到新根后立即被重置回根 0；改为 initRef 一次性初始化 + 切换序号防竞态

### Changed（变更）
- **知识图谱 Obsidian/siyuan 风格重构**（client/KnowledgeGraph.jsx）：
  - similar 内容相似边默认关闭（graphInc 默认 ["links","tags"]），阈值提高（common≥12、overlap≥0.25、上限 ≤2n）
  - hover 节点 → 邻接节点+边高亮、非邻接降透明（Obsidian 核心交互）
  - 节点大小 ∝ 连接度（关联越多越大）；file 节点按顶层目录分组着色（组间色相区分，tag/目录保持专属色）
  - 缩放联动：缩小到阈值以下自动隐藏标签文字（防密）
  - 节点 >500 分批渲染（对齐 siyuan 分批策略）
- 图谱边样式区分：link 实线清晰、similar 细线淡显

## [v0.7.2] - 2026-08-11（feature/fix-products-graph-tpl）

### Added（新增）
- **模板库文件预览**：右侧预览面板支持 Word/PDF/PPT/Excel 文件（iframe 渲染，通过 officecli 服务端渲染）
- **模板库排序**：支持按类型（默认）/名称/大小三种排序方式

### Fixed（修复）
- **产物点击显示失败**：App.jsx `open` 函数合并为单次 `setTabs`，修复 React 18 批处理导致重复 tab 且第一个 tab 缺少 doc 属性的问题
- **prompt 出错时产物丢失**：server/index.mjs 在 catch 分支也执行 `waitForFlush` 并发射 `agent_summary`，确保 agent 已写入的文件仍显示在产物列表

### Changed（变更）
- **知识图谱优化**：Obsidian 风格纸面背景（minor 24px + major 120px 半透明格线），增大节点间距（linkDistance 180/280，nodeStrength -600），修复深色主题下字体/背景颜色对比度，优化悬停高亮和边透明度

## [v0.7.1] - 2026-08-10（feature/v0.7.1-editor-enhance，已发布）

### Added（新增）
- 增强编辑工具栏：更多字体选项（20+）、颜色直接显示色块预览（20种常用颜色）、标注颜色/高亮（10种）
- 批注面板：点击批注按钮显示文档批注列表
- 表格支持优化

### Fixed（修复）
- 修复批注显示：renderComments 强制启用，添加独立批注面板
- 修复目录导航：启发式提取标题（兼容非标准样式）

## [v0.6.0] - 2026-08-10（feature/knowledge-base，已发布）

### Added（新增）
- **知识库全屏模式**：Obsidian 风格知识库界面，独立全屏模式切换（顶部栏「知识库」按钮），含文件树浏览、Markdown 预览（复用 MarkdownBody）、文档信息面板（标签/双向链接/反链/标题结构）
- **知识库 Obsidian Tab**：多文档标签页（点击文件开 tab，支持切换/关闭/缓存），左栏上下分割（文件树+文档信息可拖拽），@ 到对话按钮（自动退出知识库模式→延迟插入 ChatPanel）
- **知识图谱（思维图谱）**：基于 antv G6 v5 力导向图，节点=文档（41+），边=内容相似度（自动计算 bigram 重叠，阈值≥8 且 overlap≥0.16），支持标签/目录/双向链接三种可选边类型，点击节点联动预览，图例/布局控制/缩放/拖拽
- **本地知识库索引服务（server/kb.mjs）**：扫描配置根目录的 .md 文件，解析 frontmatter/title/tags/[[wikilink]]/内联 #tag，建全文子串搜索索引（加权打分：标题×10/标签×6/标题行×4/正文×1）+ 内容相似边图谱
- **IMA 云端知识库集成**：可选代理腾讯 ima OpenAPI（凭证配置后自动连接，支持知识库列表/搜索/读取原文/笔记正文；未配置或凭证失效时返回引导提示）
- **KB API 路由**：/api/kb/status（索引状态）, /api/kb/roots（增删根目录）, /api/kb/tree（目录树）, /api/kb/search（全文搜索）, /api/kb/graph（图谱数据）, /api/kb/doc（文档详情含反链）, /api/kb/ima/*（IMA 代理）
- **Agent KB 工具**：自定义工具 `kb_search`（知识库全文搜索）+ `kb_read`（读取文档全文），通过 ResourceLoader 上下文注入使用说明，agent 可主动检索知识库资料进行分析
- **@ 引用分析链路**：知识库文档「到对话」按钮插入 `@知识库[路径@根目录名]`，agent 识别后调用 kb_read 工具读取全文进行分析（自动退出 kbMode 延迟插入）
- **根目录管理**：kb.json 配置文件持久化，前端支持添加/移除知识库根目录（不删除原文件）
- **文件树浏览**：递归目录树，文件类型图标（Icon.jsx 补 back/cloud/backlink 图标），点击文件预览，搜索结果列表模式，最近打开定位高亮
- **模版库全屏模式**：交通规划产出文件模版库（147 个模版，10 个分类：公文版面/工作汇报/研究报告/规划报告/行业研究/论文写作/会议通知/调研问卷/研究框架/图表地图），支持网格/列表视图切换，md 文件渲染预览，二进制文件可在办公模式中打开
- **模版库 API 路由**：/api/templates（分类列表+筛选）, /api/templates/content（内容预览）, /api/templates/refresh（重新扫描）

### Changed（变更）
- App.jsx 新增 kbMode 全屏模式状态（KnowledgeBase 组件渲染），与三栏布局互斥；顶部栏新增「知识库」按钮（grid 图标）
- api.js 新增 kb 系列 API 函数（kbStatus/kbTree/kbSearch/kbGraph/kbDoc/kbIma*）
- agent.mjs customTools 数组扩展：[officeTool, kbSearchTool, kbReadTool]；ResourceLoader 上下文新增知识库使用说明
- client/package.json 新增依赖 @antv/g6 ^5.1.1
- Icon.jsx 新增 back/cloud/backlink 三个图标

## [v0.5.0] - 2026-08-10（feature/v0.5.0-office-viewer，已发布）

### Added（新增）
- Office Viewer 集成（对标 VSCode cweijan.vscode-office 插件）：
  - **docx-preview**：Word 高保真渲染（分页/表格/图片/样式），支持 `renderComments`（文档内批注显示）与 `renderChanges`（修订痕迹显示），可切换
  - **pptxviewjs**：PPT Canvas 渲染，支持翻页/跳页/页码导航
  - 新增 `/api/doc/:file/raw` 原始文件流接口（正则路由，兼容中文文件名）
- 文档路由全部改为正则路由（修复 Express 5 中 `:file` 吞掉 `/raw` 等后缀导致中文文件名匹配失败的问题）

## [v0.3.2] - 2026-08-10（feature/v0.3.1 迭代）

### Added（新增）
- 工作计划展示板（PlanBoard）：从 agent 回复中的 `- [ ] / - [x]` 任务列表自动解析为可勾选的任务板（opencode 风格，带进度条）
- 推理强度选择：对话设置栏新增 ⚡快速/⚖标准/🧠深度 三档（映射 pi `setThinkingLevel`）
- 对话设置栏：新建会话/Office/创作/模型选择移至输入区下方

### Fixed（修复）
- 修复 "Agent is already processing" 报错：entry.busy 与 pi 内部 isStreaming 不同步（收尾/compaction 期）导致的竞态，加 try-catch 重试走 steer 队列
- 修复创作模式产物不显示：agent 上下文强制写入工作区（office-workspace），否则 snapshotWorkspace 检测不到

### Changed（变更）
- 批注重设计（Word 风格）：点击批注 → 定位段落 + 整段黄底高亮 + 段落旁编号徽标（锚点）+ 滚动居中；关键词提取从批注指令中匹配正文，匹配失败回退首段
- chat-head 精简：只保留标题/连接状态/当前文件提示

## [v0.3.1] - 2026-08-10（feature/v0.3.1）

### Added（新增）
- 智能体广场：4 个面向交通规划工程师的专用助手（公文报告编制/地图可视化/文本审查/数据分析可视化），一键 @ 到对话栏（角色指令 + 依赖技能）
- `AGENT.MD` 长期记忆文件：项目工作准则（命名规范/数据校验/工作流/编码准则），参考 CLAUDE.md 制定，保持简短
- Word 式批注：点击批注后，在文档段落内以黄底标记高亮匹配文本（TreeWalker 文本级定位）

### Fixed（修复）
- 修复技能按钮点击无弹窗：`useMemo` 在 `if (!open) return null` 之后调用违反 Hooks 规则，已移动到条件之前

### Changed（变更）
- 最近会话栏移到整个对话面板最顶部（chat-head 之上）
- 全局滚动条统一风格：自定义细滚动条（8px、圆角、hover 高亮），暗/亮主题用 CSS 变量自动适配

## [v0.3.0] - 2026-08-10（develop 分支）

### Added（新增）
- 统一图标库：新增 `Icon.jsx`（线性 SVG 风格），替换全部散落 emoji/字母图标（文件类型/菜单/操作/状态/头像）
- 多主题支持：暗色（默认）+ 亮色主题，顶部栏一键切换，localStorage 记忆
- 用户/Agent 头像：圆形头像，用户为蓝色系、agent 为青绿色
- 技能广场分类：按交通规划/文档报告/图表可视化/图像生成/媒体内容/Office办公/开发工具/搜索研究分类展示
- 技能集合（工作流）：预置 4 个面向交通规划工程师的组合工作流（规划报告全流程/OD数据分析/公交规划专项/汇报材料速成），一键启动
- 会话历史按文档过滤：历史标签页「当前文档」按钮，只显示与当前打开文件相关的会话
- 附件导入扩展：支持 docx/xlsx/pptx/md/pdf/txt/csv/json 附件上传（服务端保存到工作区，agent 可读取）
- 对话栏顶部最近会话栏：显示最近 5 条会话，点击直接加载
- MD/文本文件批注：从 agent 会话提取针对该文件的修改指令作为批注记录，可在文本预览右侧面板显示并定位

### Changed（变更）
- 对话气泡统一尺寸：text/thinking/tool 块统一宽度与圆角
- 上传按钮区增加「附件」按钮，发送按钮/停止按钮改用图标
- `/api/sessions` 支持 `?file=` 参数过滤
- `/api/doc/:file/comments` 对 md/txt 返回会话衍生的修订记录

## [v0.2.0] - 2026-08-10

### Added（新增）
- MD 文件目录浮窗插件：打开 .md 文件时左侧显示可折叠目录，点击标题可定位到对应段落，滚动时自动高亮当前标题
- 对话总结产物可点击：本轮对话中的文件产物（📄 标签）点击后直接在中间文件预览区打开
- 文件侧栏右键菜单：支持「在文件管理器中打开 / 复制文件名 / @到对话中 / 删除」
- 批注定位与高亮：批注面板点击后自动滚动并高亮对应段落（3 秒脉冲动画）
- 批注手动刷新按钮（🔄），修改文件后可点击重新拉取批注
- 全局错误边界：组件渲染异常时显示友好错误页，不再直接白屏
- SSE 断线自动重连机制

### Fixed（修复）
- 修复对话后页面整页刷新白屏：添加错误边界 + mountedRef 状态保护 + 竞态延迟
- 修复 MD 文件无法滚动：修正 `.mdview-container` / `.docview` 高度链与 overflow
- 修复目录浮窗无法定位：改进标题 ID 生成（对齐 rehype-slug），点击跳转改为 ID+文本双重查找
- 修复文件管理器打开失败：服务端 `/api/open-in-explorer` 改用 ESM import + `cmd /c explorer /select`

### Changed（变更）
- SSE 连接支持自动重连（断线 3 秒后重试）
- 文件修改后批注自动重新拉取（fetchComments 抽离为可复用回调）

## [v0.1.0] - 2026-07

### Added（新增）
- Office Agent Web 初版：pi agent + officecli 集成
- 对话流分块渲染（思考/工具/文本独立气泡）、流式输出、复制/耗时/折叠
- 工作区管理（默认工作区/自定义路径）、会话历史（新建/重命名/删除/加载）
- 文件多标签打开（.docx/.xlsx/.pptx/.md/.html）
- MD 渲染增强（GFM/公式 KaTeX/代码高亮/标题锚点）
- Excel 多 sheet 查看与单元格编辑
- 技能管理弹层（@调用/导入/导出 skills）
- 双模式切换（📝 Office 编辑 / 🎨 创作）
- 对话总结条（产物清单）
- 文件实时预览（watch 模式）、批注查询面板

### Fixed（修复）
- 修复 ChatPanel TDZ 错误导致前端白屏（patch 移到 scheduleFlush 之前）
- 修复 MD 滚动（删除 CSS 重复覆盖）
- 修复 XLSX 多 sheet 加载、历史过滤 pi 会话
