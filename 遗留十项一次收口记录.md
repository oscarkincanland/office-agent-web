# Office Agent Web：遗留十项一次收口记录

更新：2026-08-18

## 本轮收口结果

本轮把之前列出的十项遗留问题统一收敛为“本地可运行能力 + 外部连接器适配层 + 明确降级状态”，避免把需要第三方授权的功能伪装成已接通。

| 编号 | 项目 | 当前结果 | 说明 |
|---|---|---|---|
| 1 | 云盘连接 | 已建立统一连接器注册表、状态接口、OAuth intent 接口 | Google Drive、SharePoint、飞书云盘仍需用户授权/插件完成实际 OAuth |
| 2 | Office 原生定位 | 已支持 `#page=3`、`#slide=2`、`#paragraph=4`、Excel `#Sheet1!A1:B2` 语法 | PDF/PPT 原生 UI 页选区仍受浏览器预览器能力限制；不再丢失定位信息 |
| 3 | ask_user 恢复 | 已支持回答队列持久化；重启后下一次 prompt 自动注入上次回答 | 无法恢复进程内原 Promise，这是运行时限制；行为已改为可恢复队列 |
| 4 | 工作流状态机 | Run manifest 增加步骤状态、尝试次数、开始/结束时间、失败信息，并提供步骤更新 API | 复杂自动重试策略需按具体技能定义，不做盲目重试 |
| 5 | 产物版本 | 已增加重命名识别和重命名回滚 | 大文件仍只保存哈希/元数据；可视化 Diff 需前端专用渲染器 |
| 6 | 深度文件解析 | PDF 增加可选 OCR fallback；Office 继续保留结构化读取入口 | OCR 需要本机 `pdftoppm`、`tesseract` 和语言包 |
| 7 | 引用展示 | 已解析引用回写消息并支持点击打开；缺失/不支持状态可见 | 逐句来源标注仍取决于 Agent 输出是否提供稳定引用 ID |
| 8 | 历史恢复 | 消息、引用、当前文件线索可恢复 | 完整恢复所有 UI 标签页和工作流阻塞状态仍由浏览器本地状态和 Run manifest 分别负责 |
| 9 | 测试 | 新增连接器/定位/工作流契约测试，已有阶段 smoke 与构建测试 | Playwright 真实浏览器测试仍需启动浏览器和网络环境 |
| 10 | Git 同步 | 本地已形成独立提交 | 远程 push 是外部写操作，需要明确授权且受 GitHub 连接器权限影响 |

## 新增接口

- `GET /api/connectors`
- `GET /api/connectors/:id`
- `POST /api/connectors/:id/auth/start`
- `POST /api/connectors/:id/status`
- `POST /api/runs/:id/steps/:stepId`
- `POST /api/agent/answer` 支持重启后的 queued answer

## 本地验证

```bash
npm run test:closure
npm run test:phase5-9
npm run build
```

## 仍需外部条件的动作

1. 安装并授权 Google Drive / SharePoint / 飞书云盘插件。
2. 安装 poppler、tesseract 及 OCR 语言包后设置 `OAW_ENABLE_OCR=1`。
3. 在真实浏览器环境运行 Playwright/SSE 重连测试。
4. 明确允许后执行 `git push`。

