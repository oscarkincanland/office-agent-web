# 规聚第七阶段真实回归与交付报告

日期：2026-09-01

版本：v0.9.33

范围：Web 项目；不包含 Electron 桌面端、Pi 内核源码修改和真实模型长回合压测。

## 一、完成内容

1. 建立 `npm run test:phase7` 真实回归入口，测试全部使用临时工作区和临时运行记录。
2. 补足并发写入、取消清理、服务重启恢复、Runtime 错误、模型错误分类和 Office CLI 不可用测试。
3. 生成并验收真实 DOCX、XLSX、PPTX、PDF 样本。
4. 使用可见 Chromium 回归 Chat / Agent 模式、多会话切换、对话历史抽屉、后台任务、未读徽标和任务中心详情。
5. 事件流增加跨进程写入锁，保护全局序号和断线重放游标。

## 二、测试记录

| 检查项 | 结果 | 证据 |
|---|---|---|
| 阶段七自动化回归 | 通过 | `phase7 regression: ok` |
| 并发文件写入 | 通过 | 第二个 Run 返回 `WRITE_CONFLICT` |
| 取消清理 | 通过 | `status=cancelled`、`staging=discarded`、锁数 0 |
| 服务重启恢复 | 通过 | Run 进入 `recovering`，写入 `run_recovered` |
| 模型错误边界 | 通过 | 超时/429 可重试；401/403 不重试 |
| Office CLI 不可用 | 通过 | 缺失路径返回结构化不可用结果 |
| DOCX / XLSX | 通过 | 结构、内容、视觉验收通过 |
| PPTX | 部分通过 | 内容、视觉通过；OpenXML 结构保留人工复核 |
| PDF | 按边界通过 | 文件头和页数通过；视觉保留人工复核 |
| 浏览器关键路径 | 通过 | Chat、Agent、多会话、历史、未读、任务中心均通过；无 page error |
| 前端构建 | 通过 | Vite 生产构建完成 |

## 三、已知边界

- 本阶段没有调用真实付费模型进行长回合压测，因此无法替代供应商网络、限流和长时间工具链压力验证。
- Pi 当前仍不提供 token 级 checkpoint；恢复仍是 JSONL 会话重开 + 恢复提示。
- PDF 视觉验收和 PPTX OpenXML 结构的人工复核仍需在实际交付前完成。
- 远程分支推送和远程仓库显示核对尚未执行，等待用户确认后再做。

## 四、交付状态

- 本地版本：`v0.9.33`
- 工作区代码：已提交并打 `v0.9.33` tag
- 远程交付：待用户确认
